[mcp_servers.ownward]
command = "bin/ownward.exe"
args = ["mcp", "--data-dir", ".ownward/development"]
cwd = "."
required = true
startup_timeout_sec = 120
tool_timeout_sec = 120

[features]
hooks = true

[agents]
max_concurrent_threads_per_session = 2
