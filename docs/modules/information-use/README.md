# 信息使用协作

产品定位以[产品需求](../../product/requirements.md#信息使用能力)为准，运行边界以[架构总纲](../../architecture/overview.md#信息使用层的运行边界)为准。

背景、核心思路及已验证方案的效果与取舍，统一见[信息使用能力探索记录](../../research/information-use-exploration.md)。

持续提出新机制、对照验证与取舍，遵循[信息使用探索方法](../../engineering/information-use-exploration.md)；方法服务于找到有效实现，不预设固定流程。

## 职责

外部智能体始终主导用户任务，并以自身智能执行各轮理解、推理、核验和取舍。Ownward 提供帮助它组织上下文、分配材料、衔接工作和交换依据的协作能力；不内置模型，不另设代行用户任务的智能体，也不以确定性代码判断开放语义。

信息已经取得后的处理流程是独立设计对象：可以隔离检索过程与任务执行上下文、分配不同工作、围绕实际分歧开展定向交流。应选择真实任务中质量与成本更好的安排，不强制简单任务进入多轮流程，不预定某种工作区、判断图或固定步骤。

## 接入实现与边界

[信息使用组件](../../../integrations/python/ownward_information_use.py)是一份零依赖 Python 接入实现，由外部智能体的运行环境执行。流程为“忠实理解与初稿 → 依据原文、理解和初稿提出有据竞争方案 → 依据原任务比较并必要修订”；不为求异改变任务或编造事实，没有成立的替代方案则保留初稿，生成与取舍分别完成。随后找回比较过程中遗漏的有用结果；只有确有补充时，才在独立上下文中依据原文核验，通过后附加，既有结果不再重写。所有理解、生成和判断都由调用方的智能完成；代码只传递材料、应用明确修订及已获认可的补充，不保存长期资产，也不调用任何供应商。

标准入口为 `respond(observations, invoke)`：第一次响应同时提供协作能力说明 `OFFER`，由智能体返回当前工作与 `use_information_use` 布尔选择。选择协作即复用当前工作进入处理流程；选择直接完成时，同一外部智能在独立上下文中只核对任务是否需要协作，不重新作答。核对接收原任务、全部已获材料及暂定结果；暂定结果只用于暴露处理中的取舍，不作事实依据。智能体依据是否存在实质解释、冲突或不完整信息的取舍，明确返回 `direct` 或 `collaborate`；简单明确的任务保留原答。代码不判断语义，也不按题号选择。

已有工具检索循环的接入方，将同一 `OFFER` 和 `RESPONSE` 契约加入原响应，再调用 `finish(observations, response, invoke)`，共用上述分派流程。`observations` 含原始 `task` 和带 `id`、`origin`、`content` 的全部已获 `sources`；不得用模型重述替换原文。返回 `used_information_use` 记录是否实际启动；经过独立核对时，`routing` 保留决定与依据。旧的显式 `complete(observations, invoke, draft=None)` 与 `reconsider(observations, draft, reading, invoke)` 保持可用，适合由调用方明确要求协作或接续已有工作。

返回的 `answer` 为最终结果，`base_answer` 保留原有纠偏后的结果，`addition` 只含获准补充；有补充提案时，`addition_review` 留下独立判断。无缺失结果就不追加解释；补充核验只接收原始任务、原文与提案，不接收旧答案的判断，以免继承同一偏差。

交付前，`route-dependency-check` 只接收原需求、已完成工作和已有判断依据，由外部智能决定是否还需要核对结论所依赖的事实关联；不重新作答，也不把工作记录当作事实。`dependency_routing` 保留选择和依据；未选择核验时原结果逐字保留。选择核验才调用 `check-dependencies`，交付原需求、全部已获原文和完成工作，不交付分派理由；外部智能保持已表达事实不变，检验去掉未证实关联后哪些结果仍成立，仅提出必要的精确修订。`work_before_dependency_check`、`dependency_review`、`dependency_edits` 分别保留核验前工作、原判断和实际修改；没有修改就不改写结果。

无现成初稿的完整流程调用六次，有补充提案时为七次；按需依赖核验另加一次，已有初稿可省去一次。单独的依赖核验不会替代原有理解、竞争与取舍流程。

精确修改无法定位时，`repair-edit-anchors` 交付选定工作、带编号的原文片段、既定修改与校验错误；调用方智能选择起止编号，代码截取原文，避免再次抄写长文本。修改内容与依据保持不变，重新校验范围、唯一性与不重叠后应用；`decision` 保留原判断，`applied_edits` 留下实际执行位置。正常修改不增加调用；纠正后仍无法应用则明确失败，不静默跳过修改。

依赖核验中的定位纠错使用独立的 `repair-dependency-edit-anchors` 阶段，共用相同定位实现，避免覆盖先前比较修订的检查点。

`invoke(stage, instruction, payload, schema)` 由接入方实现，为各阶段提供独立上下文、自己的模型和既定档位、正式协作规则、结构化结果校验及过程留痕；无现成初稿时需要支持两路并发。组件不接受标答或判分，不根据结果重试，不按题号或模型选择处理路径；服务失败和无法应用的修订明确抛出，恢复由接入方沿用已有机制处理。

产品采用[能力默认接入、流程按需启动](../../product/requirements.md#信息使用能力)的使用方式，已在标准 Python 入口及[模拟接入转接](../../../benchmarks/support/information_use_flow.py)实现。关卡与最终测试共用的 Reader 在现有响应中默认提供选择，获取信息后按同一 `finish` 契约分派；仅传入实际成功返回的工具材料，不再检索，不传入标答。各处理阶段沿用 Reader 的模型、档位、正式规则和现有检查点，用量累计，最终结果另行留存。第三方智能体需要装配此 Python 接入，单独连接信息内核的 MCP 不会自动执行客户端流程；产品不内置模型。

处理实现可独立演进，调用方只依赖入口与结果契约，无须依赖内部阶段数量。原响应主动选择协作为六次调用，有补充为七次；启动依赖核验再增加一次。原响应选择直接完成时增加一次独立分派核对，确认直接完成共两次；转入协作共七次或八次，依赖核验同样按需另加一次。流程选择不生成答案，所有智能仍由接入方提供。

## 当前效果

2026-09-08 按需依赖核验增量：231的选择两次均进入核验，两次独立核验产物均判对；固定六题及132、222在相同选择规则下保留原文本。共享组件与实际适配回放一致，138项工程检查通过；复用了未改上游和相同条件的旧判分，不是重新检索或新增全流程成绩。233仍待处理，证据及取舍见[探索记录](../../research/information-use-exploration.md#2026-09-08先核事实依据再处理任务)。

以下保留此前增量的验效依据，不代表新增依赖核验已覆盖所有场景。

2026-09-08 修订交接修复：132、222原有语义修改正确但文本位置抄错，改为由调用方智能选择原文编号后均完成并判对；222同时验证适配层在超时转换中保留未完成工作。原判断流程不变，固定六题及187、289穿过实际适配的请求、结果回放一致；这是等价性验证，不是新增八题模型成绩。语义候选未达到保底标准，未采用；逐题证据见[修复记录](../../research/information-use-exploration.md#2026-09-08四题的修订交接与任务依据)。

2026-09-08 中断恢复修复：保留原有竞争方案与后续流程，接入方在同一步骤超时重试时调用`stage_prompt(instruction, payload, working_notes)`，将未完成工作作为可纠错笔记交回自身智能续接。模拟Reader已自动接入，普通成功调用不增加步骤；模型、档位、正式规则、时限和重试次数不变。187、289均完成且判对，固定六题82、114、116、123、361、370全部判对；复用未改上游，具体成本和验证边界见[探索记录](../../research/information-use-exploration.md#2026-09-08低效推演专项)。

此前增量版首次验收在 Qwen3.8 Flash／Reader xhigh、原始材料、正式规则与判分不变的条件下，补回 112 遗漏的 $3,750 筹款归属，并明确其与关联活动总额、直接个人金额的区别；89通过，97当时尚未通过、后续复验已关闭。82、114、116保底为3／3，最终文本与原有通过结果逐字一致；82的无依据事件合并补充被独立核验拒绝。

本轮固定复用未改动的原版四步，新增步骤使用首次有效调用，不算独立全流程重复。十项边界测试通过，六个现场经实际模拟入口回放，与实测逐项一致；合计判分 5／6，不能据此宣称整批问题已解决或获得 500 题成绩。实现身份及逐题证据见[验收记录](../../../.tmp/information-use-product/admitted-release-verification.json)，探索取舍统一见[探索记录](../../research/information-use-exploration.md)。

## 验证依据

2026-09-08 按需触发修复：原先未触发的9题，冻结各自原始检索产物与首次响应，使用同一 Qwen3.8 Flash／xhigh、正式规则和最终分派实现，**9／9实际进入组件首阶段后立即截停**。对照317保持直接返回，304因解释取舍进入协作；不能外推为零误触发或答题通过。组件、公共运行器和关卡转接共86项工程检查通过，原处理函数未改；原始痕迹见[触发验证](../../../.tmp/information-use-product/routing-fix-verification-20260908.json)。

按需接入的工程验证：组件 13 项、公共运行器 66 项、关卡接入 14 项测试全部通过，覆盖直接返回、智能体选择协作、原工作复用、原始材料交接、规则与档位保持、用量及结果留痕。此验证证明接入行为，不把模拟测试代替真实模型的触发质量或全套题目成绩；此前六题效果仍属于显式启动的处理流程。

2026-09-07 默认入口实测：固定原始已获材料、Qwen3.8 Flash／xhigh 和正式规则，全新运行 `respond`，不强制开启。82、116 自主选择协作，114 选择直接完成，官方判分 **3／3**；116 主结果从初稿 $220 纠正为 $720。三题并行总耗时 743 秒，分别为 743、144、672 秒；未重跑检索，不外推为全场景触发可靠性。原始选择、各阶段与判分见[本轮现场](../../../.tmp/information-use-product/adaptive-core3-20260907-171544/summary.json)。

使用智能体实际取得的原始材料及原始任务，同模型、同档位比较任务完成质量与成本；推理阶段不提供参考答案、答案来源标签或判分反馈。保留首次有效结果与过程证据，结合事实复核，避免把判分波动当作能力提升。

组件迭代以已解决的 **82、114、116、123、361、370** 为唯一[固定保底集](../../engineering/information-use-exploration.md#组件迭代基线与固定保底集)：新增收益与六题保底全部通过后可交付增量版，未关闭问题继续登记。未修改的上游可依照探索方法固定复用，不能冒充独立重复；固定24题属于整批问题处理完、续跑最终测试前的内核验收，当前问题与下一步见[迭代任务](../../tasks/kernel-v2-final-test-failure-analysis.md)。
