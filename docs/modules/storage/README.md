# 信息存储与增长控制

本文说明 Ownward 当前如何保存一条信息、各类派生数据与长期信息资产的关系、实际存储成本，以及第一版完成前必须解决的增长问题。

长期方向与第一版架构边界见[《存储架构》](architecture.md)。

## 存储原则

> 信息资产随用户内容线性增长；派生数据具有明确、受控的单条成本，不因更新和失效状态无限堆积，并且任何派生数据都可以安全清理和重建。

长期信息资产是唯一权威。向量、关系、语义分析和索引只为组织与检索服务，不得替代资产，也不得成为恢复资产的必要条件。

## 一条信息的存储结构

| 内容 | 磁盘持久化 | 与初始文本的关系 |
|---|---|---|
| 长期信息资产 | 是 | 保存文本、稳定 ID、明确场景和明确关系，是唯一权威。当前实现会去除文本首尾空白，因此尚不是严格的逐字节原样保存。 |
| 语义派生状态 | 是 | 保存摘要、主题、线索、推导场景、推导关系和向量，可由长期信息资产重新生成。 |
| 词法索引 | 否 | 启动时根据资产在内存中构建词项和倒排表，不形成额外磁盘文本副本。 |
| 向量索引 | 否 | 启动时读取已持久化的向量，在内存中建立检索结构。 |
| 关系图索引 | 否 | 根据派生状态中的关系，在内存中建立正向和反向导航结构；关系边不复制完整文本。 |
| 向量模型 | 是，固定一份 | 整个产品共享，不随信息数量增长。 |

磁盘上主要保存两部分，而不是为词法、向量、关系和场景分别复制一份完整文本：

```text
长期信息资产
└─ 原始内容与不可丢失的明确语义

派生状态
├─ 向量
├─ 摘要、主题和线索
├─ 推导场景
└─ 推导关系
```

词法索引、向量检索结构和关系图导航结构主要存在于运行内存中，并分别从长期信息资产和派生状态构建。

---

## 当前体积基线

现有候选 `78a5198` 在同机十万条、384 维、短合成文本条件下的实测结果为：

| 项目 | 体积 |
|---|---:|
| 长期信息资产 | 26.77 MiB |
| 派生状态 | 176.77 MiB |
| 完整存储占用 | 210.03 MiB |

最终选定的 EmbeddingGemma 使用 512 维 `float32` 向量，每条固定占用 2048 字节，十万条向量约为 195.3 MiB，比原 384 维基线增加约 48.8 MiB。按现有其余结构不变估算，十万条完整存储约为 253～259 MiB；该数字尚未经过最终方案实测，不能作为完成证据。

对于很短的信息，固定长度向量和派生元数据可能使完整存储达到原始文本的数倍；文本越长，向量的固定成本占比越低，整体放大比例也越低。EmbeddingGemma 模型与运行时约 358.5 MiB，是一次性固定成本，不随信息条数增加。

## 当前增长风险

- 信息资产采用追加日志，每次更新保存完整新版本；高频修改会持续积累历史版本。
- 派生状态更新同样追加记录，虽然能够整体清空并重建，但目前没有自动压缩和失效版本回收机制。
- Q8 压缩的是模型权重，当前向量仍按 `float32` 保存；不能把模型量化误认为索引已经量化。
- 现有正式资源证据基于 384 维，不能证明最终 512 维配置满足存储前沿。

## 第一版完成前

1. 明确信息资产的保真边界；若要求原始输入逐字节不变，必须消除当前首尾空白归一化造成的变化。
2. 接入最终 512 维向量方案后，按实际产品格式重新测量不同信息规模和更新频率下的磁盘与内存占用。
3. 为派生状态建立安全的压缩、失效回收或重建机制，只保留当前有效状态，避免旧向量和旧关系无限堆积。
4. 明确信息资产历史版本的保留边界，在资产耐久性、可恢复性和长期存储成本之间形成可验证规则。
5. 只有在检索质量没有不可接受损失时，才允许采用 `float16`、整数向量或其他存储压缩方案。
6. 使用最终模型、维度、关系和更新负载重新通过同类存储前沿验收，不能沿用旧配置的通过结论。

## 依据

- [架构总纲](../../architecture/overview.md)
- [第一版交付定义](../../delivery/first-version-delivery-definition.md)
- [当前长期资产实现](../../../internal/assetlog/store.go)
- [当前派生状态实现](../../../internal/derived/store.go)
- [当前资源前沿验收](../../../benchmarks/resource_frontier/README.md)
