# 有界内存存储与检索改造待办

目标：按[架构设计](../modules/storage/bounded-runtime-design.md)消除全库驻内存和启动重建，保留原文、检索质量及既有功能。本文维护执行顺序、待办和完成状态；技术合同与验收标准以架构设计为准。

状态（2026-09-17）：前置验证与四个执行单元均已完成，单元四经独立审查、集中修复及完整复审后关闭。正式代码已接入新架构；现有用户资料尚未执行切换，未启动500题。

## 前置验证

### 价值与边界

前置验证用于以最小成本发现会迫使架构更换关键机制或技术路线的问题，避免完成整体实现后才返工。它只回答：**尚未证实的关键机制，在既定质量、资源与体验约束下是否具备实施可行性？**

它不回答完整产品是否已经实现、所有功能与故障场景是否通过、是否可以发布；不提前实现一整套产品，也不以小样代替正式验收。正式运行时集成及完整接入测试归单元一，并发控制与恢复归单元三，迁移及整体交付归单元四。

前置验证不计入执行单元。复用已有材料与有效证据，不重做语义准备或整套题集；证据足以决定实施或修改机制即停止，不追加穷尽验证。已验证内容仅在直接依赖变化或出现反证时重验。

### 已取得的证据

- [x] **内存超限根因与隔离修复。** 相同编译器和输入下，仅修正无用缓冲分配，进程树峰值由664.78 MiB降至204.97 MiB；6组输入、39个向量逐字节一致。测量含连接器和5 MiB工作区，仅证明隔离修复成立；正式运行时集成结果见单元一。[验证证据](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-assembly-recovery-20260916.json)。
- [x] **数据库基础行为。** 同一隔离小样通过原文保真、回滚、读快照、检查点及完整性校验；不代表完整并发调度和故障恢复已经通过。[验证证据](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-assembly-recovery-20260916.json)。
- [x] **向量筛选离线初检。** 复用4,734条真实向量，13个探针的Top-10均与实验参照一致；采用双精度计分及理想页面布局，尚不能证明正式计分与实际落盘成本。[初检证据](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-filter-design-evidence.json)。

### 其余两项机制验证结论

- [x] **落盘向量检索具备实施价值。** 复用4,734条真实向量，13个查询的候选、排序和分数均与正式 `derived.Index.Search` 完全一致。采用有界批读后，相比落盘精确扫描，平均SQLite读取量由11.67 MB降至3.59 MB，查询由25.78毫秒降至19.14毫秒；查询专用进程峰值18.10 MiB。原全驻内存查询约0.69毫秒，落盘仍增加本地读取成本，不能将筛选收益表述为比原内存实现更快。
- [x] **现有SDK支持有界流式接入机制。** 贯通stdio协议、HTTP及结构化／文本双份响应；约4 KiB、1 MiB、16 MiB的JSON传输负载均保持原文字节及相邻字段一致。最大负载对应约8.44 MiB原文，流式处理峰值15.93 MiB、耗时2.65秒，普通SDK对照为337.03 MiB、3.72秒；小负载增加约1.8毫秒。大负载无需整包进入SDK内存。

两项均取得支持实施的证据，可以进入单元一。[测量、边界及复现材料](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-remaining-preflight-20260916.json)。向量读量包含真实SQLite页面开销，操作系统缓存未清空；流式验证是同进程管道与本机HTTP的隔离机制切片，未包含本地模型。以上不代替完整产品内存、冻结负载耗时及功能验收，相关检查随既定执行单元完成，不另开前置验证轮次。

按相同资料密度与筛选效果估算，累计原文500 MB、1 GB、10 GB时，向量查询约为0.19、0.38、3.84秒；这些是温缓存条件推算，冷读还受磁盘带宽及随机访问影响，不是已验证的性能保证。详细读量和假设见证据，不准备大规模新材料。

本节只维护验证结论与证据；依赖变化或出现反证时更新对应结论和顶部状态，已经完成的内容不得继续列为待办。

## 单元一：有界存取底座

目标：贯通有界的保存、按ID读取与更新。对应架构第2～4节及第5.1节的权威发布部分。

- [x] 将已验证的内存修复机制接入正式构建，生成相互兼容的运行时制品并重算清单与空间身份；落实共享预算和固定连接池。
- [x] 实现SQLite权威资产、控制状态、分块正文、定向读取端口、原子发布及操作回执。
- [x] 贯通连接器、HTTP和工具的流式输入输出，保持权限、修订、取消和接续语义。
- [x] **验证并关闭。** 核对正式调用路径和修复范围；保持模型、精度及计算配置不变，与修复前正式运行时逐字节比较短／长查询、中英文资料及批量的向量输出，保存源补丁、构建配置与制品身份。原文逐字节一致，重复请求不重复执行，大小输入不整包加载，完整进程树峰值保留规定余量。此单元不承担自然语言检索交付。

运行时仅采纳EmbeddingGemma嵌入模式的无用词预测缓冲修复，以及保留管道和生命周期管理的Windows无控制台启动。实验用C接口简化服务和模型类型裁剪不进入正式版，实验制品身份及内存成绩不代替正式构建的验证。

验收：完整运行时的6组、39个向量与修复前逐字节一致；新格式的创建、批量、读取和更新通过真实stdio→连接器→HTTP→工具→SQLite链路。最大输入原文约10.24 MiB，结构化及文本结果均保真，重复操作不重复创建；4线程条件下服务、模型及连接器合计峰值217.46 MiB，距270 MiB上限留有52.54 MiB。未设置额外Go内存上限。全仓Go测试通过，并验证原有依据身份、权限撤销、过期版本、取消回收与协议引用隔离。[证据与复现入口](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-unit-one-20260916.json)。

独立复审（2026-09-16）发现并修复RPC编号转义后无法匹配回执的问题：统一按SDK逻辑身份匹配，HTTP、stdio、客户端及重复编号隔离测试通过。修复后受影响接入测试、静态检查与清单校验通过，随后对固定版本重新完成整轮独立审查，期间源码未变；发现的特殊字符规范编码、批量回执逐项恢复两项缺陷已集中修复并通过自验；修复后的固定版本完整复审通过，无剩余阻塞项，单元一关闭。测量、审查与修复版本身份分别保留在上述证据中。

新格式入口为 `core.StreamingAssets` 与 `mcpserver.NewStreamingStorage`；连接器仅在服务声明对应能力后启用流式路径。单元一验收覆盖已实现的四项存取工具，完整检索、语义组织、维护、控制界面与旧资料切换仍按后续单元完成，不将此成绩表述为完整产品迁移验收。

## 单元二：持久化检索与语义组织

目标：完成保存→组织→检索→取证路径，保持既有质量。对应架构第5节及第6节的有效性边界。

- [x] 实现持久化词法、向量精确筛选、图导航与依据读取，替换相应全库物化调用。
- [x] 接入语义工作、依赖、世代及组织发布；原文和派生索引按既定事务生效，新增向量立即可查。
- [x] 完成向量首轮构块、增量归块、成员原子切换及必要的触发与背压；查询合并活动块与增量区，归块前后不漏查、不重复计分。
- [x] **验证并关闭。** 同材料下候选、评分、排序、过滤与依据等价；覆盖连续新增、更新及归块切换，旧修订及失效派生不能交付，完整路径资源与耗时达标。

验收（2026-09-16）：固定原文、组织产物及向量下，词法、组合检索、图导航、定位与语义规范化等价；4,734条既有向量、13个查询在归块前后均与原内存实现的候选、分数和顺序逐项一致。已覆盖组织发布、重复提交、输入变更、连续新增、归块替换及损坏筛选的精确回退。全仓Go测试、静态检查与组合清单校验通过；独立审查发现的交付互锁、已接受语义重复派发、明确关系优先、更新后导航顺序及累计组织内存边界已修复。最终版本受影响包测试通过，固定版本完整复审通过，212项源码身份首尾一致，无剩余阻塞项，单元二关闭。

以下资源与向量测量保留修复前的版本身份，未改写为修复后的新实测；最终验收身份与复审报告另行记录在同一证据中。完整链路经真实stdio→连接器→HTTP→工具→SQLite与既定本地模型验证，约4 KiB、1 MiB、8 MiB原文和依据保真。进程树峰值211.16 MiB，非模型峰值51.40 MiB；依据实测将非模型内部预留由48调整为56 MiB，产品270 MiB总上限与独立余量不变。4 KiB资料的检索约0.19秒，8 MiB资料约11.03秒；后者包含长原文依据定位，不能套用短查询阈值，也不代表发布p95。温缓存下落盘向量筛选平均87.11毫秒，逻辑载荷读取量与完整阶段耗时见[版本身份、实测与复现材料](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-unit-two-20260916.json)。验证使用固定组织结果，不增加外部AI调用，不冒充14／24题或整体迁移验收。

## 单元三：维护、遗忘与故障恢复

目标：长期使用和中断恢复时，数据有效性与资源边界持续成立。对应架构第6～7节。

- [x] 完成有界依赖失效、遗忘清理及版本回收，接通已登记的持久维护任务。
- [x] 实现WAL调度、空间归还、重启接续及备份恢复；将单元二已有的增量归块与其他维护工作统一调度，保障持续前台工作下的维护进展。
- [x] **验证并关闭。** 并发修订、撤销和中断不复活旧资料；覆盖长读、持续写入与撤销并存，控制操作与WAL维护在预算内完成；反复更新后内存、日志与磁盘积压收敛，启动不重载全库。

实现与自验（2026-09-17）：维护任务按批接续，前台工作区与维护轮转、控制提交优先；退休正文与组织、旧世代、向量块和过期游标得到回收，超额空闲页增量归还。遗忘屏障立即影响读取、派生有效性和词法统计，清理正文、受污染派生、旧日志及受控快照；恢复旧快照前合并当前删除账本，保留体系与主体身份，使旧凭据失效。组织发布的轻量版本身份独立于回收正文，避免清理干扰有效提交。SQLite格式升级为2，旧程序不能绕过新屏障；驱动升级至兼容的1.40.1，修复取消查询时结果对象未释放的问题。

固定版本全仓测试通过；维护专项覆盖连续40次修订、未发布暂存的重启清理、遗忘期间迟到提交、组织回收与发布交错、旧世代回收、快照校验及长读占用下的WAL控制。40次修订后仅保留当前正文，数据库约2.2 MiB；长读与持续控制写入的WAL峰值约30.3 MiB，单次实测增长约0.58 MiB。实测与版本身份见[单元三验证记录](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-unit-three-20260917.json)。未重新准备语义材料或调用外部AI；这些是确定性维护与接入验证，不代替单元四的整体交付验收。

独立审查（2026-09-17）确认的5项缺陷已集中修复：遗忘后的向量块残留、恢复租约作用域、永久失效组织的回收、控制插队后的轮转，以及恢复删除账本时的词法统计。最终版本全仓测试、静态检查和组合校验通过；固定版本完整复审通过，9项独立测试全过，345项源码身份前后一致，无剩余阻塞项，单元三关闭。上述初次维护测量保留修复前的版本身份，不改写为修复后的新实测；最终源码、两轮审查及验证记录保存在同一证据中。

## 单元四：旧资料迁移与整体交付

目标：安全切换旧资料，在同一最终版本完成交付。对应架构第8、10节。

- [x] 实现旧格式流式迁移、新旧实例互斥、切换及各中断位置的恢复。
- [x] 适配可复用测试材料，保留原文、控制状态和有效语义材料；按空间兼容条件迁移向量，清理被替代的生产路径。
- [x] 完成[原文存入、组织与派生数据](../modules/storage/ingestion-and-derived-data.md)的全篇交付核对：更新流程、产物表和保存关系图，明确原文、派生数据及检索索引在SQLite中的持久化位置与关联，说明独立文件的边界、有界缓存及重启读取方式；旧实现的耗时与容量数据不得作为新实现现状。
- [x] **验证并关闭。** 迁移完整、中断可接续、旧程序不能双写；同一最终版本通过架构第10节规定的功能、质量、资源与成本验收，保存版本身份和证据。

实现与验证（2026-09-17）：正式装配已接入SQLite、授权控制、流式工具、备份恢复和部署交接。旧资料在原锁下隔离、校验并切换，6个耐久边界可恢复，激活前可取消、激活后只能向前恢复；旧程序拒绝标记永久保留。真实资料暴露的词法建索引自持读事务阻塞WAL收敛问题已修复，迁移不新增AI调用。全仓Go测试、静态检查、组合身份校验及材料迁移校验通过。

存储接入版本 `4e7a236f` 先完成固定14题，再完成固定24题：**11／14、24／24**，平均完整等待分别108.29秒、71.14秒。14题原始失败为114、123、289；123沿用已放弃题登记，仍保留原始分数。114和289的12次实际查询在旧内存实现中重放，返回内容、排序、分数和证据位置均一致；不能将这两题直接归为存储迁移回归。首批6题在作答和判分后因缺少历史提交凭据中断了报告生成，补齐原凭据后离线完成报告，答案及判分哈希未变，新增AI调用为0。

**成本收尾：** 词法倒排改用整数文档版本编号，48份真实资料的数据库由8.749 MB降至6.210 MB（减少29.0%），原文、组织、向量及22,359条词项／频次校验一致。小型临时数据在共享预算内使用最多8 KiB缓冲，超出即落盘，原有磁盘额度、流式处理及耐久写入不变；建库表结构在一个事务中发布。SQLite格式升级至3，旧索引分批转换、检查点接续并归还空闲页，旧程序拒绝打开。

冻结资源负载、相同本地模型及4线程条件下：完整进程树峰值210.52 MiB，非模型49.58 MiB；20次热查询p95约547毫秒；模型预热后的20份批量存入约312毫秒／份；三次冷启动2.02～2.86秒，均满足对应边界。模型冷启动的另一轮首次批量约698毫秒／份，单独保存，不作为预热成绩。新增索引体积单独核算，未用旧编码比例冒充整个数据库成本。

**成本收尾核验：** 组合 `3face7f6` 的全仓Go测试、静态检查、组合校验与准备材料迁移测试通过。物理存储和缓冲调整后，先回放14题，再回放24题：407次实际产品调用的返回内容、分数、顺序、图结果及依据定位全部一致；3次原宿主在调用产品前拒绝的请求单列，不计产品回放。导航接续使用新签发的令牌实际执行，不比较随机令牌字面值。每题从保留的原始准备经正式入口独立迁移，原文、组织及向量验证等价，没有新增外部AI调用。该确定性回放验证受影响路径，原11／14、24／24成绩仍绑定原评测版本，不改写为最终版本新跑出的成绩。

**独立审查与关闭：** 已修复目标激活状态不明时错误回退、授权管理者交接被误拒绝两项缺陷。最终组合 `52797ade` 的受影响三包66项顶层测试及静态检查通过；完整独立复审通过，6项顶层、7项子测试全过，387项文件身份首尾一致，无剩余阻塞项，单元四关闭。本轮仅修正迁移取消和交接授权，上述资源、工具回放和模型成绩保留原测量身份。既有信息使用短板不被表述为已解决；用户资料未切换，不自动续跑500题。[源码身份、迁移证明、回归及成本证据](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-unit-four-20260917.json)。

## 执行边界

每个单元实现并验证通过后再进入下一单元；授权、版本、原子可见与停止使用检查随各条路径落实。四个单元不是独立发布版本，迁移交付前不切换现有用户资料，不开放尚未完成的能力。

各单元改变正式代码行为时，同次更新上述数据说明文档的受影响部分；单元四关闭前完成全篇一致性核对，未实现的设计不得提前写成现状。

复用有效验证成果，正式接入核对实际调用路径；各单元优先进行受影响路径的确定性验证，最终回归集中在单元四，不重复语义准备或逐单元全量跑题。完成后停止，不自动续跑500题。
