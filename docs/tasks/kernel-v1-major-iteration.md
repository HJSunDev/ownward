# V1 内核大版本迭代

状态：V0 正式失败问题池、首个可证根因簇、唯一负责方向、专属快速视图与 V0 基线已经建立；尚未实施 V1 内核优化，当前最佳产品候选仍为 V0。

## 职责

本文是 V1 大版本的唯一调度状态，只维护上一基线、当前候选、五个方向的视图、门槛、状态与证据，以及当前根因和下一验证点。机器结果仍由 Acceptance Suite 保存，本文只引用，不复制正式题目、答案、源会话或逐题证据。

固定依据为[内核演进体系](../engineering/kernel-evolution-system.md)、[V0 档案](../engineering/kernel-versions/v0.md)、[产品需求](../product/requirements.md)、[整体架构](../architecture/overview.md)和[验收体系](../delivery/acceptance-system.md)。

## V0 基线

- 唯一候选：`99f519018df99bd5202b0c571b8e43481cd1b80e`；当前 HEAD 相对该候选的 `internal/`、产品入口、`go.mod`、`go.sum` 零变化，快速观察使用逐字等价产品源码，未把后续验收工具变化计入内核。
- 正式 LongMemEval‑S：500 题正确 242、错误 258。正式诊断摘要为 `15bb1dfc…f6d72`，报告摘要为 `3e7355c6…89d9`。
- 全部 258 个错误已进入 `.tmp/kernel-v1-major-iteration/problem-pool.json`（`c325eae8…d69456`），每项只保存正式身份、首次观测缺口、可证机制状态、负责方向、内部样本和证据路径/摘要。

## 正式失败映射

| 映射 | 数量 | 结论 | 状态 |
| --- | ---: | --- | --- |
| 会话级检索单元超过上下文预算 | 119 | 语义提交完整、目标资产均已组织且被搜索返回，但回答所需源会话正文合计超过固定 24,000 字符；这是首个可证通用机制 | 当前根因簇 |
| 预算内仍未读取目标证据 | 14 | 首次缺口位于排序或上下文选择，证据不足以判定唯一根因 | 待补证 |
| 搜索未返回目标证据 | 116 | 只能证明检索阶段首次缺口，仍可能来自语义组织质量或检索实现 | 跨方向待补证 |
| 完整读取后 Reader 仍答错 | 9 | 当前证据不支持归为内核根因 | Reader 边界保护项 |

问题池没有把“未搜索到”或“未读取”直接写成根因，也不含正式题目文本、产品答案、Gold 身份和源会话内容。

## 当前业务问题

- 场景：外部智能体回答跨会话、时间或更新问题，需要同时取得多个来源事实。
- 预期：内核在固定检索预算内交付完整、可追溯且可组合的必要证据。
- 实际：V0 以完整会话作为不可分检索单元；119 个错误即使已找全目标会话，其必要正文自身仍无法同时进入 24,000 字符上下文，最后一项或多项证据因预算被截断。
- 原因链：会话级权威资产同时承担检索单元 → 单元过粗 → 必要证据总量超过上下文 → 已命中的目标证据无法被读取 → 回答失败。
- 目标状态：保持原始资产、稳定身份和来源不变，同时让内核提供更细、源绑定、可重建的证据单元，使必要事实能在同一预算内完整交付。本轮只冻结问题与验证，不预定具体实现。

## 五个方向

| 方向 | 快速视图与门槛 | 状态 | 证据/下一点 |
| --- | --- | --- | --- |
| 信息表示与组织结构 | `organization-granularity-v1`；预算召回由 `0.3889` 至少提高到 `0.7778`，错误率由 `0.6111` 降至不高于 `0.3056`；保护项不得退化 | **进行中，唯一启动方向** | 119 项首根因；下一包形成最小通用候选并运行同一视图 |
| 检索架构与算法 | 尚未冻结；不得复用首方向改善冒充本方向关闭 | 未启动 | 14 项待补证，首方向关闭后再裁决 |
| 语义能力与表示模型 | 尚未冻结 | 未启动 | 116 项跨方向待补证，不能先行归因 |
| 数据结构与存储架构 | 尚未冻结 | 未启动 | 只在首方向方案暴露规模/重建瓶颈后建立视图 |
| 执行架构与状态维护 | 尚未冻结 | 未启动 | 只在实测证明执行链限制质量或效率后建立视图 |

## 快速视图与保护边界

- 冻结材料：[`benchmarks/acceptance/suite/materials/optimization/v1/direction-organization-granularity.json`](../../benchmarks/acceptance/suite/materials/optimization/v1/direction-organization-granularity.json)。三项主样本覆盖多来源、更新和有序关系，两项专属反例保护预算内单/多证据；共用保护继续复用 core v1，不建立平行体系。
- 样本事实、答案和表达与正式 500 题不重合；每次生成问题池都会扫描全部 258 项诊断和源资产日志，并拒绝禁止字段或合成标记碰撞。
- V0 快速基线：预算召回 `0.3888888889`，错误率 `0.6111111111`；预算内保护、身份稳定、融合召回和融合 NDCG 均为 `1.0`，三次重复误差为 `0`。
- 淘汰条件：主指标未达到冻结的大幅改善门槛，或任一保护指标低于 V0，候选立即淘汰；快速结果不晋升正式基线。

## 入口、成本与恢复

唯一非正式入口：

```powershell
python benchmarks/acceptance/suite/run.py kernel-iteration `
  --formal-run E:\Ownward\acceptance\longmemeval-s\runs\99f5190\formal `
  --output .tmp\kernel-v1-major-iteration `
  --candidate 99f519018df99bd5202b0c571b8e43481cd1b80e `
  --resume
```

- 首方向视图实测 `0.674 s`，共用保护检查 `0.632 s`；分别低于 3 分钟，合计远低于 5～7 分钟目标和 10 分钟上限。
- 问题池、方向观察和保护观察按正式证据摘要、材料、产品源码树和工具身份分层；身份相同则逐字节复用，变化时只重跑失效部分。
- `.tmp/kernel-v1-major-iteration/resume-proof.json` 记录四个检查点二次恢复前后摘要不变；正式 Acceptance Suite state 未被读取或修改。

## 下一工作包

只继续“信息表示与组织结构”：从当前首根因设计并实现最小通用候选，保持权威资产与公开能力合同不变，通过同一快速视图证明预算内必要证据交付大幅改善，同时保持全部保护指标。达到门槛前不得切换方向、运行完整内核模式、产品资格/全量验收或 LongMemEval‑S。
