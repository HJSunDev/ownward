# V1 内核大版本迭代

状态：V1 候选 `24b847da7d8fdf05b5cb22b7e0b904f53dd8cc96` 的首次整体验证未通过；数据结构与存储架构方向因完整模式索引资源保护回归重新打开。V0 仍是唯一正式基线，V1 尚未晋升。

## 职责

本文是 V1 大版本的唯一调度状态，只维护上一基线、当前候选、五个方向的视图、门槛、状态与证据，以及当前根因和下一验证点。机器结果仍由 Acceptance Suite 保存，本文只引用，不复制正式题目、答案、源会话或逐题证据。

固定依据为[内核演进体系](../engineering/kernel-evolution-system.md)、[V0 档案](../engineering/kernel-versions/v0.md)、[产品需求](../product/requirements.md)、[整体架构](../architecture/overview.md)和[验收体系](../delivery/acceptance-system.md)。

## V0 基线与当前候选

- 唯一正式候选：`99f519018df99bd5202b0c571b8e43481cd1b80e`。
- 五项已提交成果：按需细粒度证据 `e6bfc82`、长资产语义表示 `436e12c`、来源排名—证据深度调度 `d19c745`、紧凑耐久状态 `c9c8b48`、批量耐久状态切换 `24b847d`。
- 当前冻结候选为 `24b847da7d8fdf05b5cb22b7e0b904f53dd8cc96`，发布二进制摘要 `77db9ac8…9296`；唯一 state 已正式重绑该候选，V0 基线保留，V1 尚无通过检查点。
- 正式 LongMemEval‑S：500 题正确 242、错误 258；诊断摘要 `15bb1dfc…f6d72`，报告摘要 `3e7355c6…89d9`。

## 正式质量问题池

| 首个可证机制 | 数量 | 状态 |
| --- | ---: | --- |
| 会话级检索单元超过上下文预算 | 119 | 按需细粒度证据链已关闭 |
| 预算内目标证据因前缀贪心未读 | 14 | 来源排名—证据深度有界公平装载链已关闭 |
| 长资产语义向量表示缺失 | 116 | 长资产语义表示链已关闭 |
| 完整读取后 Reader 仍答错 | 9 | Reader 边界保护；无证据时不得归因给内核 |

同一正式失败可以在不同阶段暴露多个现象，因此表中机制数量不作互斥求和。质量方向的关闭只绑定通用机制、内部不重合视图和当前候选保护结果，不把社区诊断的相关性写成最终根因。

## 第五方向业务问题与根因

- 场景：用户或接入智能体一次沉淀一批信息时，批量调用应保持逐项身份和耐久语义，但不应把同一批确认退化为每项两次串行磁盘屏障；后台重建的不可见世代也不应在每条派生记录后强制落盘。
- V0 表象：500 题共创建 `23,867` 项资产、形成 `1,498` 个语义批次；产品创建阶段累计 `2,750.038 s`，平均 `5.500 s/题`、p95 `9.593 s/题`。语义/Reader/裁判分别累计 `36,992.627 s`、`5,295.681 s`、`3,562.214 s`，属于模型与外部评测成本，不计入本根因；逐题隔离、审计和测试调度同样排除。
- 机械根因：公开 `CreateBatch` 先逐项调用权威资产 `Create`，再逐项写入待处理派生记录；每项写入都独立 `fsync`。完整派生世代已经与当前状态隔离，却仍逐记录 `fsync`，直到最后才封存和原子切换。批量和世代事务边界没有传递到持久层，造成可消除的串行耐久等待。
- 负责方向：执行架构与状态维护。该根因不属于正文存储放大、模型推理、语义质量或检索质量，前四方向只作为交叉保护。

## 五个方向

| 方向 | 快速视图与关闭门槛 | 状态 | 当前候选证据 |
| --- | --- | --- | --- |
| 信息表示与组织结构 | `organization-granularity-v1`：预算召回至少 `0.7778`、错误率不高于 `0.3056`，规模、质量和相对成本无退化 | **已关闭** | 当前候选召回 `1.0`、错误率 `0`，全部保护通过 |
| 检索架构与算法 | `budget-fit-evidence-selection-v2`：必要证据项/问题召回 `1.0`、错误率 `0`，排序、预算、读取、恢复及成本保护通过 | **已关闭** | 当前候选全部门槛与前三方向交叉保护通过 |
| 语义能力与表示模型 | `semantic-representation-v1`：语义召回 `1.0`、错误率 `0`，短资产、单向量、输入规模、查询、重启和重建无退化 | **已关闭** | 当前候选全部门槛与细粒度证据保护通过 |
| 数据结构与存储架构 | `storage-deposition-v1`：产品与派生存储放大均不高于 V0 同输入的 `0.5`；权威恢复日志回收比例不高于 `0.5`；质量、查询、恢复和耐久保护通过 | **重新打开** | 定向门槛仍通过，但完整模式 `index_bytes` 从 V0 的 `529,592` 增至 `530,488`，按冻结保护合同构成 `896 B` 资源回归 |
| 执行架构与状态维护 | `durable-batch-state-transition-v1`：批量耐久写入与世代构建成本均不高于前驱同输入的 `0.5`；公开创建、内存、并发、顺序、耐久、恢复及前四方向保护通过 | **已关闭** | 批量耐久成本比 `0.135`，世代构建成本比 `0.319`；重复误差上界分别为 `0.144`、`0.332`，全部保护通过 |

其余四个方向的关闭证据仍绑定当前候选；数据结构与存储架构必须先消除完整模式索引资源回归，才能再次进入整体验证。尚未运行资格验证或正式 LongMemEval‑S，也不得把快速结果冒充正式基线。

## 存储方向冻结视图

- 视图：[`direction-storage-deposition.json`](../../benchmarks/acceptance/suite/materials/optimization/v1/direction-storage-deposition.json)，摘要 `a6bd58a5…8609`。
- 输入：只使用与正式事实和表达不重合的合成资产；长度覆盖 V0 当前资产的最小值、p25、p50、p75、p90、p95、p99 和最大值。正式目录只做只读规模审计，不进入候选执行。
- 主门槛：产品总存储和派生存储至少减半；权威恢复日志在反复更新后至少回收一半；每资产最多一个当前派生记录。
- 保护：公共语义工作恢复、重复提交幂等、重复维护字节稳定、备份恢复、搜索召回均为 `1.0`；查询 p95 不超过 `600 ms`；前三方向的完整快速视图继续通过。
- 淘汰条件：任一存储、质量、查询、耐久、恢复或已关闭方向保护失败即淘汰；快速结果不晋升正式基线。

## 存储方向架构与兼容边界

- 权威资产仍是唯一事实来源。活动恢复日志原子压实为每项当前资产一个快照；稳定 ID、当前 revision、正文、明确语义和来源不变。产品没有声明历史版本能力，因此旧完整文本版本不是永久资产；显式备份仍能完整恢复当前权威资产。
- 派生格式升级为 `ownward.derived/v4`：语义工作只持久化资产/候选 ID、revision、相似度、上一分析和工作身份；公开工作在读取时按原顺序从权威资产无损重建。
- 已接受提交只持久化能力身份、状态、接受时间和规范提交摘要；当前分析保留一份，重复提交通过摘要保持幂等，不再复制第二份完整分析。
- 松散 v3 状态首次打开时自动转换并原子压实；已封存世代继续沿现有完整重建、校验和原子切换路径迁移。没有双写或永久旧格式写入路径。
- 维护只保留每资产一个当前派生记录；反复维护结果字节稳定。更新失效、关闭重开、向量恢复、世代重建、备份和空白恢复均继续成立。
- 按需证据身份直接对已经验证的 UTF‑8 正文字节取摘要，删除 `string → []rune → string` 的等价往返分配；证据 ID 字节和公开合同不变，确保已关闭检索链在当前候选上满足相对成本保护。

## 存储方向入口、结果与恢复

唯一非正式入口：

```powershell
python benchmarks/acceptance/suite/run.py kernel-storage `
  --formal-run E:\Ownward\acceptance\longmemeval-s\runs\99f5190\formal `
  --output .tmp\kernel-v1-major-iteration\storage-deposition-v1-final-2 `
  --candidate worktree `
  --resume
```

- `candidate-result.json`（摘要 `3b0acfba…3949`，结果身份 `d784dbec…bda6`）通过；正式只读存储观察摘要为 `d186162c…2a0`。
- 同输入候选的总存储放大比 `0.0708536`、派生存储放大比 `0.0142966`、权威历史回收比 `0.4179915`；每资产一个当前派生记录，搜索召回、语义工作恢复、提交幂等、重复维护和备份恢复均为 `1.0`，存储视图查询 p95 为 `8.33 ms`。
- 存储视图墙钟 `1.598 s`，连同语义、信息粒度、core 和预算选择保护共 `10.917 s`，低于单视图 3 分钟和方向 10 分钟上限。
- 相同候选、材料、正式审计和观察器身份 `--resume` 时，五类观察全部直接复用，关键检查点逐字不变；正式 Acceptance Suite state 未被读取或修改。
- 全仓 Go 测试与构建、Acceptance Suite 121 项单元测试、core/product/product-resource 适配器测试、LongMemEval‑S 34 项测试、Suite check 与 self-check 均通过。

## 执行状态方向冻结视图

- 视图：[`direction-execution-state.json`](../../benchmarks/acceptance/suite/materials/optimization/v1/direction-execution-state.json)，绑定前驱提交 `c9c8b48` 与 V0 聚合成本；只使用 18 项合成事实并确定性扩展为 54 项资产、三个自然批次，覆盖正式长度边界，不读取题面、答案、Gold 或正式正文。
- 主门槛：同输入批量耐久写入与完整世代构建均至少减半；三次重复误差计入上界后仍须达标。
- 保护：权威资产、派生记录、批内顺序、关闭重开、中断尾部恢复、未提交世代隔离、并发批次、公开搜索均为 `1.0`；批量上限保持 `20`，分配量不超过前驱加 `25%`，公开批量创建 p95 加重复误差不超过 `600 ms`；前四方向全部通过。
- 淘汰条件：任一成本、正确性、并发、耐久、恢复、内存、尾延迟或已关闭方向保护失败即淘汰；快速结果不写正式 state、不晋升基线。

## 执行状态终态架构

- `CreateBatch` 先逐项完成输入、关系和身份校验，再以一个有界权威日志批次写入；只有整批日志完成一次耐久屏障后才发布内存状态。输入级错误仍逐项返回，已确认结果在返回前全部持久化。
- 新资产的待处理派生状态保持原有候选顺序和语义工作身份：内核用隔离的批内索引合并既有与本批语义候选，校验全部记录后由 `PutBatch` 一次持久化，再发布运行索引，避免未落盘状态对并发读取可见。
- 完整派生世代在独立、不可见目录中顺序构建，不再逐记录落盘；封存时统一 `fsync`、校验记录与资产快照、写入清单，最后原子切换当前指针。失败或竞争继续丢弃候选世代并保留上一代。
- 单项创建、更新和已发布世代的增量写入仍保持逐项耐久；日志格式、稳定身份、迁移、备份和公开合同均未改变，不存在双写或永久兼容路径。

## 执行状态入口、结果与恢复

唯一非正式入口：

```powershell
python benchmarks/acceptance/suite/run.py kernel-execution `
  --formal-run E:\Ownward\acceptance\longmemeval-s\runs\99f5190\formal `
  --output .tmp\kernel-v1-major-iteration\execution-state-v1 `
  --candidate worktree `
  --resume
```

- `candidate-result.json`（摘要 `5d16e129…a9f2`，结果身份 `272bd2f6…a943`）通过；正式只读执行观察摘要为 `de2c9ac6…a56d`，执行观察摘要为 `3d569375…0ea7`。
- 同输入三次实测：批量耐久写入 p95 从 `128.24 ms` 降至 `18.06 ms`，比值 `0.135`、含重复误差上界 `0.144`；世代构建从 `89.79 ms` 降至 `27.58 ms`，比值 `0.319`、含误差上界 `0.332`。
- 公开批量创建 p95 为 `114.73 ms`、含误差 `161.43 ms`；分配量比 `1.007`、含误差 `1.163`。正确性、并发、耐久、恢复、隔离、搜索及前四方向保护全部通过。
- 执行视图墙钟 `3.076 s`，连同前四方向保护共 `7.522 s`；`resume-proof.json` 证明相同候选、材料、正式聚合观察和工具身份逐项复用且结果字节不变，失效部分才重跑；正式 Acceptance Suite state 未读取或修改。

## 下一工作包

首次正式完整模式在生成报告前发现执行器把 `efficiency`、`semantic_representation`、`storage_architecture`、`execution_state` 四个方向专用阶段误并入固定完整材料。当前待提交修复已将完整模式恢复为固定 11 阶段，方向专用阶段仍只允许 `targeted` 使用，并保留非正式工作树来源证明；定向测试和同一候选的隔离完整观察均通过。隔离观察位于 `.tmp/v1-holistic-fix-check/frontier-observation.json`，摘要 `043985d9…7318`。

修复后的完整观察与 V0 机械比较为 `rejected_regression`：全部质量指标保持，`index_build_ms` 改善约 `0.504 ms`，但 `index_bytes` 增加 `896 B`，违反受保护资源门槛。未运行 core、qualification、full 或正式 LongMemEval‑S。

下一工作包先独立复核并提交验收执行器修复，再在数据结构与存储架构方向定位并消除 `derived.NewIndex` 当前状态表示/规范化路径相关的索引分配回归，同时保护已关闭四方向。修复形成新候选后重新绑定，只重跑失效的完整模式；通过后才能继续 core、qualification 和 full。
