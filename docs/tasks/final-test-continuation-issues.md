# 最终测试续跑问题收集

## 遇到问题如何处理

**每遇到一个失败，先按[四方向归因](../delivery/acceptance-system.md#最终测试失败归因)查清原因，再登记到下方对应列表；任一列表本批累计达到 10 道不同题，立即停止投放新题，保留并收齐在途结果，转入本批问题处理，不再续跑。三个列表分别计数，不按合计数触发。**

- **内核问题：** 必要信息未被可靠维护、获取或忠实交付，修复信息内核的通用短板。
- **组件问题：** 必要信息已交付，但处理流程未帮助外部智能正确使用，迭代通用的信息使用能力。
- **组件问题必须记录实际触发状态：未触发，归入触发机制问题；已触发，归入组件处理能力问题。** 以运行痕迹中的 `information_use.used` 为依据，两类共同计入组件列表的10题阈值，不另设列表。
- **需放弃的问题：** 复核完整原文后，预期答案仍缺乏合理依据；数据有冲突本身不构成弃题理由。达到阈值后集中复核并完成处置，不为命中不合理标答修改产品。
- **模型能力仍是第四个归因方向：** 当前模型已单独验证能力可用，不设累计列表；不能据此强行把任何失败塞入前三类。若出现明确反证，应记录证据并重新核实能力前提。
- 归因必须有实际交付材料、回答与判分依据；判分误判、服务失败、未完成不冒充上述缺陷，保留现场并处理。尚未定因的题不得漏掉或强行分类，先完成归因。
- 按原始题号去重；同题多次失败更新同一条，若存在两个独立根因，可分别登记并注明关联。条目只保留核心原因、证据入口和处置状态，不粘贴长日志。
- **三个列表只维护当前待处理问题，条目只保留一份；迁入持久化登记或其他权威文档后，立即从本列表删除并更新计数，不留重复条目或迁移说明。** 已触发的停跑决定不因迁出而撤销；不得用迁出来规避10题停跑阈值。本批处理完成后，按[迭代策略](kernel-v2-final-test-failure-analysis.md#后续迭代策略真题补短板固定集守能力)从实际完成位置的下一题继续。

### 组件问题：先修触发，再验效果

测试默认采用6题并发。完整验证按需触发与处理效果，通过后保存结论并移出待处理列表。

- **未触发只证明调用缺口，不能据此判断组件能力无效。** 先集中解决未触发问题，再处理已触发但执行失败或结果不合格的问题。
- 触发机制根据任务中使用信息的实际困难按需启动，提炼通用条件，提高适当场景下的触发可靠性；不按题号定制、不全量强制启动，不追求所有任务100%触发。
- **触发修复阶段只验是否实际进入组件流程。** 固定模型、档位和组件处理能力，做最小验证；观察到真实调用启动并留存证据后立即停止该次实验，不继续消耗资源生成完整结果或判分。未触发则继续优化触发机制；口头表示需要组件不算触发，主动截停不算执行失败。
- 触发验证通过仅关闭触发缺口，条目标为“已触发，效果待验”，不能记作整题解决或直接删除。随后统一验证处理效果：通过的保存结论后移出；执行失败或结果不合格的转为组件处理能力问题，继续修复。已有完整有效结果直接复用。
- 两阶段状态更新同一条记录，不重复计数；中断实验以实际调用启动痕迹为触发依据，不要求已有完整运行才生成的 `information_use.used` 结果。

## 起点与维护边界

**2026-09-08按用户要求紧急暂停，测试及其子进程已停止。本批377–407题已完成：30对、1错（397）；408–413题中断，现场保留；414–500题尚未启动。恢复时复用已完成结果，先续接408–413，再投放414起的题，默认6题并发。三个列表仍按本批累计，任一列表达到10题停止新增投放；第233题保留在[固定登记](kernel-v2-final-test-failure-analysis.md#尚未解决的问题登记)，不重复计入新批次。**

- 开发覆盖1–376题均已有有效结果，0题待补跑；已发现问题除8道弃题外仅233未解决。[补齐验证证据](../../.tmp/kernel-fast-iteration/candidate-05-boundary/reader-replay-20260908-191110/summary.json)。
- 原运行的 `paused.json`、`failure.json` 与逐题结果保留原貌；后续专项补跑通过不覆盖历史失败。当前开发完成状态结合专项复验记录核对，不把旧失败文件误认成待补跑项，也不拼成同一版本正式成绩。

本批从原始 **第377题**开始收集；前376题不重复跑，8道弃题见[固定弃题登记](kernel-v2-final-test-failure-analysis.md#不再以答对为优化目标的题目登记)。

续跑接在上批实际完成的最后一题之后，不重跑已处理的失败题。并发收尾可能使最后完成的题不是失败题，仍按实际完成位置衔接，不退回最早失败处；实际续跑位置以上方维护状态为准。

本文是后续问题积累与停跑阈值的唯一维护位置；历史结论、固定回归集和整体迭代策略由原迭代文档维护。弃题仍保留正式测试原始判分，不删题、不改分。

本批运行标识：`kernel-fast-candidate-05-boundary-formal-20260908-194920`，现场位于执行配置指定的运行目录。启动入口 `.tmp/kernel-fast-iteration/continue_classified.py` 复用公共运行器，每次投放前读取三个列表计数；以本文件10题分类阈值为准，禁用原启动器7题总失败阈值。旧运行现场与专项复验独立保留，不拼成正式成绩。

超时或执行中断必须检查失败步骤、流式输出、重试请求及续接记录，区分内容拒绝、服务故障、未续接重复推理与组件执行缺陷；有最终判分后另判结果质量。不得仅凭“timeout”登记为服务问题并搁置，不能把未完成计为通过。

## 1. 内核问题

本批累计：**0／10**。

| 原始题号／ID | 内核根因 | 证据入口 | 状态／处置 |
| --- | --- | --- | --- |

## 2. 更好地使用数据的组件问题

本批累计：**1／10**。

| 原始题号／ID | 实际触发／优化方向 | 信息使用根因 | 证据入口 | 状态／处置 |
| --- | --- | --- | --- | --- |
| 397／`2133c1b5` | 已触发／组件处理能力 | 近期“在原宿住了3个月”与旧公寓住满1个月的自述均已交付，初稿已采用近期3个月并保留冲突；组件要求近期自述必须明说“同一公寓”，并排除同日较晚记录，改用旧记录推算7个月。冲突取舍时增加未要求的证据限制，推翻有依据的初稿；非内核缺失，模型已有可行初稿，标答有近期自述支持。 | 本批现场 `questions/2133c1b5/reader/codex/complete.json`、`reader/codex/information-use-result.json`、`answer.json`、`judge/output.json`；完整原文 `answer_52382508_1`消息4、`answer_52382508_2`消息2。 | 原判错保留，待统一迭代。组件整体已触发；内部依赖核验未启动，不能标成组件未触发。 |

## 3. 答案不符合正常预期需放弃的问题

本批累计：**0／10**。


| 原始题号／ID | 预期答案不合理的依据 | 完整原文复核入口 | 状态／处置 |
| --- | --- | --- | --- |
