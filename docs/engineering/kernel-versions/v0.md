# 信息内核 V0

V0 是内核首次完整验收后的冻结基线，不代表第一版已经完成。

| 项目 | 内容 |
| --- | --- |
| 代码候选 | `99f519018df99bd5202b0c571b8e43481cd1b80e` |
| 发布二进制 | `77600820a450b0931b9e63595b8564be757825000af16f9c2f0a028c69e81d2b` |
| 状态 | 内部验收通过；主动检索同尺非盲比较 `20/20`，V2 因同质量下效率与资源更优成为后续唯一候选基线 |

## 实现

| 方向 | V0 实现 |
| --- | --- |
| 信息表示与组织结构 | 长期资产与派生状态分离；稳定身份承载来源与明确语义，外部语义结果形成带版本、依据、置信度和场景的关系图。 |
| 检索架构与算法 | 直接读取、词法与 512 维向量独立召回，等权倒数排名融合；高分种子进行一层关系扩展，再按场景校验、排序和截断。 |
| 语义能力与表示模型 | 外部智能体通过 `semantic_work` / `semantic_submit_batch` 提交语义候选，内核校验身份、版本、证据与冲突；EmbeddingGemma 300M Q8 提供本地向量。 |
| 数据结构与存储架构 | 无损资产日志保存权威信息；关系、向量与索引进入可重建派生世代，当前世代完整校验后原子切换，运行索引和缓存可销毁。 |
| 执行架构与状态维护 | Go 内核管理批量沉淀、语义工作、派生重建和恢复；向量运行时由 Ownward 管理，失败时保留资产及上一有效世代。 |

## 内部测试

| 范围 | 结果 |
| --- | --- |
| 核心不变量 | 资产保存、更新、备份恢复、派生重建、稳定身份、派生可销毁、模型可替换：`7/7` 通过。 |
| 内核质量（固定集） | 身份、增量一致性、合并拆分、关系精确率/召回率、图召回率、融合召回率和上下文精确率均为 `100%`；词法召回率 `97.30%`，向量召回率 `98.37%`，融合 NDCG `99.24%`。 |
| 内核效率（固定集） | 组织 P95 `0.61 ms`，索引构建 `1.72 ms`，融合查询 P95 `1.06 ms`；派生数据 `14,380 B`，索引 `529,592 B`。 |
| 产品专项 | 24 个场景全部通过，跨时间、多跳、场景适用和信息更新的召回率、精确率、NDCG 均为 `100%`；查询最长 `457.61 ms`，峰值内存 `254.67 MiB`，场景端到端最长 `21 分 14.86 秒`。 |

## 历史 LongMemEval-S 被动评测

以下运行采用官方清洗 500 题、`gpt-5.6-luna` 语义与 Reader、`gpt-5.6-terra` 裁判，但宿主固定预取并决定读取顺序，越过了外部智能体主动检索职责。数据与计分记录保留，结果只描述旧被动评测模式，不证明 V0 产品能力、内核根因或晋升资格。

| 指标 | 结果 |
| --- | --- |
| 总分 | `242/500`，准确率 `48.40%` |
| 单会话用户记忆 | `65/70`，`92.86%` |
| 单会话助手记忆 | `56/56`，`100%` |
| 多会话 | `28/133`，`21.05%` |
| 偏好 | `13/30`，`43.33%` |
| 时间推理 | `37/133`，`27.82%` |
| 知识更新 | `43/78`，`55.13%` |
| 检索时延 | 平均 `41.20 ms`，P95 `78.00 ms`，最大 `188.00 ms` |
| 完整墙钟 | `3 小时 27 分 56 秒` |

## 判断

**长板**：资产与派生状态边界可靠，恢复与演进不变量完整；固定内部场景质量高、内核查询快；单会话事实记忆稳定。

**历史被动模式观察**：258 个错误中，116 个首次缺口是固定搜索未返回目标证据，133 个是宿主固定读取未交付目标证据，另有 9 个在交付后仍答错；这些数据不能归因于产品定义的主动检索内核。该运行消耗 `96.91M` 语义输入 Token 和 `8.65 GB` Ownward 数据，仍可证明当时的组织成本。

V0 的内部确定性证据继续有效。主动检索同尺比较中，V0 为 `20/20`，累计墙钟 `289.250 s`、检索均值 `107.20 ms`、P95 `188 ms`、数据量 `2,268,840 B`；该结果不构成正式分数。完整证据见[纠偏结果](../../../benchmarks/acceptance/suite/iteration/evaluation-correction/product-faithful-comparison.json)。
