# 验收体系

## 目标体系

建立一套长期稳定的 **Ownward Acceptance Suite v1**。它独立于内核实现，内核怎么迭代都使用同一把尺子。Ownward 自有的测试数据、真值、评分器、执行配置、版本清单和结果证据随项目公开；第三方基准固定官方版本和执行协议，不擅自复制受限制内容。独立第三方能够依照公开材料复现并审查结论。

体系由一个高频优化闭环和三层验收证据组成。

### 内核前沿优化环

内核前沿基准使用版本化的固定多规模资产快照、变更序列、查询与真值，并冻结向量和语义输入，不调用外部智能能力，不重新沉淀或审查数据。固定规则以固定种子生成不改变真值的身份、顺序、时间、场景和干扰信息变体；新候选与当前有效内核基线始终运行同一批变体，防止针对固定表面形式调优。基准在相同环境下呈现二者与同期公开可复现等价前沿的差距；外部前沿只在基准版本、对标对象或环境变化时重新校准，不在每轮优化中重复运行。没有等价外部基线时，当前最佳结果只能作为内部基线，不能据此宣称达到前沿。

基准提供不超过三分钟的定向模式和不超过十分钟的完整模式：定向模式只运行受当前修改影响的能力与阶段；完整模式覆盖全部受保护能力，并在候选进入资格验证前执行。结果必须分别呈现信息身份、关系质量、错误合并与拆分、增量一致性、组织和索引成本，以及各类检索中词法、向量、关系、场景、融合阶段的质量、时效与资源变化；保留逐项证据并定位损失发生的阶段，使组织错误、召回缺失、排序错误、关系污染、场景误判和性能瓶颈能够直接进入修复。该基准参与开发调优，只承担优化反馈，不能作为最终验收证据。

质量、时效和资源分别设防，不计算可以相互补偿的综合分。新候选只有在完整模式下所有受保护能力均未发生超过重复测量误差的退化，且至少一项指标达到随基准版本预先固定的有效改善时，才能进入资格验证；资格验证通过后才成为新的有效内核基线。

### 验收证据

1. **固定内核基线**以确定性、本地、低成本验证资产保存、更新、恢复、重建、身份稳定、派生状态可销毁、模型可替换等产品不变量，不调用外部智能能力。
2. **固定 Ownward 专项数据集**覆盖跨时间、多跳关联、场景适用、信息更新和关系组织增益。每个场景必须具有完整输入、可核对证据、容纳合理语义表达的真值边界和确定的评分方法；通过一次冷启动审查后随版本冻结，不在候选验收时重新审查。第一版完整集固定为四类能力各六个场景，共二十四个场景、一百二十条信息；资格集随版本固定选取每类两个代表场景。该规模只承担第一版产品能力覆盖与回归验证，不被包装成开放世界通用性的统计证明，也不得为追求表面完整而继续扩张。

   - 资格验证使用其中固定的代表性子集。
   - 最终验收使用同一数据集的完整集合。
   - 相关实现发生变化时复用同一数据重新运行，不重新生成或重新验证数据。
   - 发现新缺陷只能在新版本中追加回归场景，不能改写既有版本。
   - 任何测试数据、真值、评分规则或执行口径变化都必须产生新的不可变版本，旧版本和旧成绩永久保留；只有产品要求或验收语义发生实质变化时才升级主版本。
3. **LongMemEval‑V2 官方公开基准**
   作为第一版唯一的社区主基准，严格使用固定版本的官方数据、协议、准确率—延迟指标和排行榜提交格式。它有 451 道人工题、5 类记忆能力、Web/企业两个领域及官方排行榜，能够给 Ownward 一个社区可比较的公开成绩。[官方仓库](https://github.com/xiaowu0162/LongMemEval-V2) Ownward 专项集只证明产品自身要求，不能替代官方成绩或由项目自行宣称行业领先。公开基准暴露的缺陷只能抽象为通用能力问题并进入新版内核前沿基准，不得复制题目、答案或为特定样本增加规则。

执行层级与触发条件固定为：

- 普通开发：只运行覆盖当前变更的定向测试。
- 影响身份、资产、派生世代、备份或恢复不变量的变化：运行固定内核基线。
- 影响组织或检索行为的变化：运行内核前沿基准定向模式。
- 一轮内核优化准备纳入有效基线：运行内核前沿基准完整模式；形成整体改善后，在相关内核基线通过的前提下运行固定资格集。
- 候选稳定：运行一次固定内核基线和完整 Ownward 专项集。
- 全部内部验证通过且最终候选冻结：对该候选最后运行一次 LongMemEval‑V2。
- 同一候选汇总三层证据，形成 Ownward Acceptance Suite v1 的最终结果。

在第一版固定规模和基准环境下，固定内核基线不得超过五分钟，内核前沿基准定向模式不得超过三分钟、完整模式不得超过十分钟，资格集不得超过三十分钟，完整内部验收不得超过九十分钟。LongMemEval‑V2 是唯一允许超过九十分钟的步骤，预计四至八小时，并且只在最终候选上执行。实际成本超出预算时立即停止并修复执行路径，不得用等待代替解决问题。

失败只使该项及依赖它的上层证据失效；修复后只重跑失效层级。不得重新生成验收数据、重复仍然有效的无关工作、对未修改候选重复执行同一验收，或把完整专项集和官方基准下沉到日常迭代。

## 用户提示词

### 审查验收体系

```text
顶级架构师的身份，以乔布斯的视角再次审查这个验收体系，静态区。是否专业，是否可行，是否能高效地把内核持续推向前沿。

有问题修复，没问题直接回复。
```

### 审查验收体系执行计划

```text
以顶级架构师的身份，以乔布斯的视角审查这个验收体系的执行计划。完整执行后是否必然以专业、可行、高效且不扩大范围的方式，将项目现状收敛为静态区已经定稿的验收体系；是否彻底清理旧错误路径、错误数据和无效产物；是否只建设并验证验收体系本身，在具备开始正式验收的全部条件后立即停止，不提前执行任何正式验收；是否不会改变产品架构、模块职责和既有功能。

有问题修复，没问题直接回复。
```

### 审查验收体系实现

```text
以顶级架构师的身份，以乔布斯的视角审查当前验收体系实现。静态区已经定稿，不得修改，也不得通过降低标准使现有实现通过审查。

检查代码、固定数据、真值、评分器、适配器、执行入口、证据生命周期、成本控制、公开说明和清理结果，判断它们是否完整、准确地实现静态区规定的验收体系；是否专业、公开、可复现；是否能够高效推动内核持续达到前沿，并以固定内核基线、固定 Ownward 专项数据集和 LongMemEval‑V2 形成可靠的三层验收证据；是否具备明确的候选与环境绑定、结果复用、局部失效、中断恢复、异常停止和基线晋升能力；是否不存在测试专用产品路径、重复入口、旧错误流程、无效数据、临时残留和无法真正执行的纸面设计。

审查与修复只能作用于验收体系实现及其直接说明，不得改变产品定位、整体架构、模块职责、正式能力契约和既有功能，不得扩大验收范围，也不得提前运行资格集、完整专项集或 LongMemEval‑V2 等正式验收。使用最小夹具、单元测试、隔离自检和非正式预检证明体系自身真实可用；不得以文档描述代替可执行实现。

有问题修复并重新审查，没问题直接回复。
```

## 执行计划

目标是把项目现状完整收敛为上述验收体系。这里只建设并验证体系本身，不执行正式验收，不判断当前产品是否通过第一版。以下事项必须依次完成；每项达到完成标志后才能进入下一项。

建设不得改变产品定位、整体架构、模块职责、生产数据格式、正式能力契约或既有功能行为。验收工具优先通过正式公开能力观察产品；确需补充可观测性时只能增加不改变行为的证据出口，不得在产品实现中加入验收专用分支、旁路能力或测试专用行为。任何需要改变产品或模块设计才能继续的事项均超出本任务边界，不得执行。

批量、模型或智能体操作在成本未知时必须先用最小样本验证路径并估算完整成本；没有独立价值的结果不得反复生成，已经有效的结果必须形成可恢复检查点。任何长时间无有效输出、重复返工或超出本体系成本边界的路径都应立即停止并修复根因。

### 执行事项

状态：`[ ]` 未完成；`[x]` 已完成；`[!]` 阻塞。进度：`100%`。预计总工时：`3.5～7 小时`。

工时按 Codex 基于当前仓库有效成果执行的实际时间估算，包含建设与体系自检，不包含正式验收、用户中断或外部阻塞等待；其中测试、构建和外部处理的硬性等待预计不超过 `1～1.5 小时`，不存在计划内的单次多小时任务。A01 完成后依据迁移清单复核一次剩余工时；任何事项预计超过上限时，必须先标为 `[!]`、说明根因并更新估算，不得无声超支。

只有对应事项的完成标志全部成立后才能标为 `[x]`；完成一项立即更新状态，进度按已完成事项数计算。后续事实一旦推翻前置事项的完成依据，必须立即将该事项及依赖它的后续事项恢复为 `[ ]`，保留仍有效的成果和检查点，从最早失效项继续，不得绕过问题或整批重做。

| 状态 | 编号 | 执行事项 | 预计工时 | 开始时间 | 必须交付的成果与完成标志 |
| ---- | ---- | -------- | -------- | -------- | ------------------------ |
| [x] | A01 | 建立迁移清单 | 10～20 分钟 | 2026-08-21 16:32 | 先停止仍在运行的旧验收、数据生成和验证进程，保留已有检查点与现场，不继续消耗资源。随后盘点现有验收代码、数据、配置、入口、流程、报告、候选包和临时产物，形成受版本控制的迁移清单，逐项标为“保留”“改造”“提取有效成果后删除”或“删除”，并标明其在目标体系中的唯一落点；清理范围不得包含产品资产、产品实现、关键工作记录和其他模块成果。**完成标志：**旧路径没有运行中的进程；所有现有事项均有明确处置和目标落点；目标体系没有无人承接的能力；不存在来源、用途或依赖不明的保留项。 |
| [x] | A02 | 冻结验收契约 | 20～40 分钟 | 2026-08-21 16:35 | 在编写或改造工具前，分别确定内核前沿优化环、固定内核基线、固定 Ownward 专项数据集和 LongMemEval‑V2 的版本标识、输入清单、输出结构、评分与判定关系、环境身份、成本边界、结果复用、失效传播、执行模式和停止条件，并形成受版本控制的契约与契约测试。**完成标志：**每一层的输入、输出、版本绑定和通过关系均可由机器验证；执行者不需要在实现过程中重新解释验收语义。 |
| [x] | A03 | 建立固定数据与基准材料 | 30～60 分钟 | 2026-08-21 16:38 | 从现有有效成果中一次性提取并整理版本化的多规模资产快照、变更序列、查询、真值、冻结后的向量与语义输入及确定性变体，以及 24 个场景、120 条信息的完整 Ownward 专项数据集和每类 2 个场景的 8 场景资格集。场景逐项冷审，通过即冻结；未通过时只修复并复审该场景，不丢弃已通过场景或整批重做。按照 A02 冻结的等价条件，只对当前公开且可复现的直接同类实现完成一次来源核对；能够在同一输入、协议和基准环境中复现时固定对标对象及版本，否则记录不可比并停止，不扩大调研，也不以换算或推测伪造前沿结论。发现覆盖缺口时只补充缺失能力，不建立候选冻结后重新生成或整批替换数据的正式路径。**完成标志：**数据、真值、评分器、配置、来源、版本、摘要和公开复现材料完整；资格集与完整集边界固定；同口径外部前沿校准记录或不可比结论完整；正式执行不再依赖数据生成器或独立数据验证会话。 |
| [x] | A04 | 实现内核前沿优化环 | 45～90 分钟 | 2026-08-21 16:43 | 实现定向模式和完整模式，对同一输入比较新候选与当前有效内核基线；存在 A03 确认的同口径外部前沿时同时呈现其差距，不存在时明确限制结论。首次尚无有效基线时，只将当前实现固定为引导参照；它必须在后续正式资格验证通过后才能成为有效基线，此前不得宣称晋升或整体改善。质量、时延与资源分别设防，不得以综合分互相补偿；结果定位到身份、关系、合并拆分、增量一致性、组织与索引，以及词法、向量、图、场景和融合阶段。完整模式无受保护能力实质退化且至少一项预先定义的能力取得实质提升时，只允许进入资格验证；资格验证通过后才能晋升有效内核基线。**完成标志：**先以带有已知故障的正反夹具证明工具能发现召回、排序、关系、场景和性能退化，且会拒绝指标互相补偿、未通过资格验证的错误晋升，再在隔离状态中对完整固定基准执行一次体系自检，实测定向模式不超过 3 分钟、完整模式不超过 10 分钟；自检不修改正式基线，不形成候选验收证据。 |
| [x] | A05 | 实现三层验收证据 | 45～90 分钟 | 2026-08-21 16:45 | 建立固定内核基线的确定性不变量验证；建立 Ownward 资格集与完整集的固定执行、评分和证据输出；固定 LongMemEval‑V2 官方版本、正式入口与结果契约，并以该版本官方提供的格式定义、样例或协议测试验证适配，不复制受限制数据。所有验收只能通过产品正式公开能力运行，不得在产品侧增加测试专用实现，也不得叠加第一版不需要的公共基准。**完成标志：**三层均以已知正确和故意破坏的最小夹具验证材料完整性、版本一致性、前置条件、评分与拒绝能力和结果结构；LongMemEval‑V2 适配与固定官方协议一致；不运行资格集、完整集或 LongMemEval‑V2 正式验收。 |
| [x] | A06 | 建立统一执行与证据生命周期 | 30～60 分钟 | 2026-08-21 16:48 | 建立唯一执行入口，按变更范围选择定向测试、内核基线、前沿优化环、资格集、完整集或 LongMemEval‑V2；统一落实成本上限、候选与环境绑定、检查点、结果复用、失效传播、基线晋升、报告汇总和停止规则。前置证据不成立时必须拒绝启动更高成本层级。**完成标志：**通过最小夹具完整验证层级选择、候选不可变绑定、结果复用、局部失效、中断恢复、超时停止、汇总一致性和非法启动拒绝；不以正式验收数据执行验证。 |
| [x] | A07 | 切换入口并统一文档 | 10～20 分钟 | 2026-08-21 16:50 | 新体系自验证通过后，将唯一执行入口切换到新体系，更新与验收直接相关的执行契约、说明和持续开发入口，移除旧路径的触发入口与直接引用；不得修改产品正式能力契约，旧实现此时只能作为待清理对象，不得继续运行。**完成标志：**代码入口、验收执行契约和文档只指向目标体系；旧路径无法再被触发；独立执行者不需要补充设计即可确定各层的运行方式、证据和停止条件。 |
| [x] | A08 | 清理旧体系并完成建设验证 | 20～40 分钟 | 2026-08-21 16:55 | 删除旧动态正式数据生成与验证路径、错误协议、重复入口、未冻结的废弃数据、虚假或未完成的临时报告，以及建设过程产生的临时数据、进程和候选包；已被目标体系认可为正式版本的历史成绩、关键工作记录和仍被目标体系使用的公开证据必须保留。随后运行验收工具单元测试、正反夹具、最小冒烟检查、受影响模块测试与构建，并在空白隔离目录中以自检模式和最小夹具依照公开说明完成一次从统一入口到结果结构的复现。最后执行不消费正式验收样本的预检，确认固定运行时、本地模型制品、外部智能体接入、官方数据取得与校验路径、隔离目录、磁盘空间和成本估算均可用。**完成标志：**旧路径无触发入口、直接引用或临时残留；保留项均有目标落点；代码、数据、入口和文档一致；相关测试、构建、隔离自检与全部预检通过；产品架构、模块职责和既有功能无变化；没有运行中的验收进程，本次建设没有生成新的正式验收报告。 |

### A01 迁移清单

| 现有事项 | 处置 | 目标落点 |
| -------- | ---- | -------- |
| `benchmarks/acceptance/v1`～`v5` | 保留 | 历史固定基线及修正证据；退出当前正式入口，不改写既有版本。 |
| `benchmarks/acceptance/fixed/`、`internal/acceptance/`、`cmd/ownward-acceptance/` | 改造 | 拆分为确定性的固定内核基线与固定 Ownward 专项数据集执行器；提取备份、恢复、身份和检索验证，移除旧唯一类型、精确关系金标和旧固定入口。 |
| `benchmarks/acceptance/dynamic/` | 提取有效成果后删除 | 提取场景结构、真值边界、评分方法和已经通过的数据；冻结为固定 Ownward 专项数据集后，删除候选冻结后生成、独立验证和整批失效的正式路径。 |
| `benchmarks/acceptance/module_lifecycle/` | 改造 | 并入固定内核基线，继续只通过正式公开能力验证存储、向量和语义生命周期。 |
| `internal/performance/`、`cmd/ownward-performance/`、`benchmarks/resource_frontier/` | 改造 | 建成内核前沿优化环及同口径外部前沿校准；对标结果按环境与版本复用，不下沉为日常重型任务。 |
| `benchmarks/agent_integration/` | 提取有效成果后删除 | 将外部智能体创建、检索、读取、更新和跨会话证据并入固定 Ownward 专项数据集，不保留第四条独立正式验收轨道。 |
| `benchmarks/delivery_resource/` | 改造 | 将完整交付资源与本地模型成本纳入固定专项证据和统一质量、时延、资源分轨结果，不保留独立完成判定。 |
| `benchmarks/longmemeval_v2/` | 保留并改造 | 固定官方版本、主动检索入口、结果契约和最低充分预检，作为唯一社区主基准。 |
| `benchmarks/final_acceptance/` | 改造 | 汇总固定内核基线、固定 Ownward 专项数据集和 LongMemEval‑V2 三层证据；删除对旧动态、消融及其他独立轨道的强制依赖。 |
| `benchmarks/support/`、`internal/candidate/` | 保留并改造 | 作为统一入口共享的正式 MCP 接入、候选二进制与环境绑定能力。 |
| `benchmarks/vector-model-evaluation/`、`docs/research/vector-model-selection.md` | 保留 | 向量模型选型的冻结研究证据，不进入验收体系执行路径。 |
| `internal/embedding/acceptance*` 与模型条款入口 | 保留 | 正式产品的模型条款确认能力，不属于验收专用实现。 |
| `OWNWARD_DISABLE_RELATIONS` 产品开关及其旧消融路径 | 删除 | 目标体系不再依赖产品侧验收专用分支；固定专项数据直接验证关系组织增益与整体能力。 |
| README、交付文档、工作台及模块文档中的旧验收引用 | 改造 | 统一指向 Ownward Acceptance Suite v1 的唯一入口、三层证据和成本规则；关键工作记录保持只增不改。 |
| `.tmp/`、候选包、未完成报告、运行缓存和旧验收进程 | 删除 | 仅在 A08 清理；保留目标体系认可的正式历史成绩和仍有目标落点的公开证据。当前未发现运行中的旧验收进程。 |

### A08 建设验证

完成时间：`2026-08-21 18:19`。

- 旧动态生成、旧固定入口、重复汇总与资源轨道、产品侧验收专用关系开关及全部临时目录已删除；历史 `v1`～`v5`、关键记录、正式性能能力和选型证据保留。
- `go test ./...`、`go build ./...`、验收体系 70 项测试、三组内部适配 13 项测试及 LongMemEval‑V2 8 项测试通过；其中两项需要正式候选或主动启用的测试按设计跳过。
- 统一入口的契约检查与空白隔离自检通过；真实内核观察器以非正式隔离材料覆盖 11 个阶段并输出 16 项质量、时效与资源指标。预检确认候选二进制、本地模型制品、Codex 接入、LongMemEval‑V2 固定官方版本取得与校验路径、E 盘隔离目录、磁盘空间和成本边界可用。
- CI 已切换到统一体系的非正式测试与自检；旧路径零触发入口和零直接引用，无验收进程、缓存或临时残留。未运行资格集、完整专项集或 LongMemEval‑V2，未生成正式验收报告。

完成边界：A01～A08 全部标为 `[x]` 且进度为 `100%`；内核前沿优化环和三层验收证据均已真实建成，旧错误路径与无效产物清理完毕，公开材料、版本、执行入口和文档一致，工具自身验证通过，产品架构、模块职责和既有功能保持不变，独立执行者无需补充设计即可开始正式验收。达到后立即停止，不得运行资格集、完整集或 LongMemEval‑V2，也不得生成正式产品验收结论。
