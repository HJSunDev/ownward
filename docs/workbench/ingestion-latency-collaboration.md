# 双任务协作

本页维护长期协作规则与当前任务；切换任务只更新动态区，有价值的结论和证据保留在历史区。

## 长期规则

| 参与方 | 任务名称与ID |
| --- | --- |
| A | ownward架构师；`01a06fb8-3494-7c11-9eac-da48a61c66ef` |
| B | 乔布斯；`01a09993-f7ba-7970-8a4a-1d03b4781a10` |

- 遵循用户确定的分工和顺序。并行探索可交流中间发现；独立审查按“理解背景 → 固定版本完整审查 → 统一报告 → 集中修复与自验 → 一次性交付复审”推进，审查期间不改被审版本、不插入复审要求。
- 使用 `mcp__codex_app__send_message_to_thread` 向对方ID交接，保持模型设置；消息只含任务、结论、证据和下一步职责。
- 同一文件由一方统一修改；重型测试协调使用资源，避免竞争，复用有效证据，不重复运行。
- 变更须经必要验证，保留可恢复成果与依据；未经授权不操作Git，不启动定时任务。
- 接续或压缩后先核对本页的当前任务、版本和阶段，历史记录不作为待办。

## 当前任务：信息可用时效·执行单元一审查

**背景与目标：** 同一权威数据上，原文、基础索引、明确关系、待办和回执原子保存后即可读取与基础检索，组织工作随后安全接续；原默认行为及深度处理能力不退化。本轮只审查、修复并关闭单元一，宿主后台执行与取用分流属于单元二、三。

**依据与对象：** [需求、产品、架构及单元一](../tasks/information-availability-and-on-demand-processing.md)、[实际存入流程](../modules/storage/ingestion-and-derived-data.md)。审查当前磁盘实现及相对HEAD的相关差异，不把暂存状态当作版本边界。基线与原验证见 `.tmp/information-availability-unit-one-20260919/`；本轮固定身份与报告保存于其 `review/` 下。

**分工与顺序：** B先理解背景，再对固定版本完成整轮独立审查并统一报告；A只读核对范围与证据，不改被审实现、不插入修复反馈。报告完整到齐后，A集中修复与自验，再一次性交付B完整复审；B不修改正式源码或权威文档。

**要求与标准：**
- 保存后原文真实可读、可查，不提前宣称组织完成，不丢已接受数据、明确关系和耐久回执。
- 领取、续期、释放、等待和提交绑定权限、体系、主体、修订及世代；覆盖互斥、遗漏唤醒、重启、更正、撤权、遗忘和过期结果拒绝，旧接入不混领。
- 升级保留原文、有效组织及待办；正式CLI／MCP、契约、制品与文档一致，默认路径不新增AI阶段或改变材料、模型及预算。
- 缺陷反绑当前可达失败与单元合同，不扩展后续单元；复用有效验证，只补必要本地证据，不跑模型评测、不提高并发、不操作Git。

**结束条件：** 完整审查发现的范围内缺陷经修复、自验和完整复审关闭，必要回归与制品校验通过，最终身份、证据及单元状态同步保存。单元一通过不等于整个需求交付。

**完成状态（2026-09-19）：** 完整独立审查、集中修复与完整复审结束，远程工具分页、完成占用阻塞新待办、部分完成向量恢复三项问题全部关闭。最终组合 `e55821c02c8b6d9e3407d04dc3cf2ee6a8161f2eebdc1bcf6ec3171aeab4e9f2`，397项被审身份首尾一致；受影响模块回归、静态检查、制品校验及B最终5个独立场景通过。见[关闭记录](../../.tmp/information-availability-unit-one-20260919/review/closure.json)与[完整复审](../../.tmp/information-availability-unit-one-20260919/review/r3/report.md)。单元一结束，不追加优化或模型评测；单元二、三尚未实施。

---

## 历史协作证据：外部智能工具与项目边界审查

**完成状态（2026-09-19）：** B完整审查、A集中修复和B完整复审结束；Qoder结束事件校验、完整响应收尾、队列截止时间和管道超时回收四项缺陷全部关闭。104项外部客户端、12项Node回归及5组独立边界验证通过，977项文件身份首尾一致，迁移与原百炼／Go兼容成立。本轮修复仅涉及项目外Qoder工具与专用测试；没有真实模型调用、正式题集续跑或Git写操作。[完整复审报告](E:/Dev/external-intelligence/review/r2/report.md)与[最终身份及关闭记录](E:/Dev/external-intelligence/review/closure.json)已保留；历史在线成绩和旧验收失败保留原边界。本任务结束，不追加优化。

---

## 历史协作证据：有界存储模块整体审查

**完成状态（2026-09-17）：** 三轮完整审查与集中修复结束，遗忘临时副本、重建恢复及相关交错问题全部关闭。最终组合 `4b53322bccaf263f5791ebc32a1a9b6df3bbd0deb2d5e0bb6bf948fda11cc03f` 经B完整复审通过，12项独立检查全过，406项文件身份首尾一致，无剩余阻塞项；A已保存[正式关闭记录与复现证据](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-module-review-20260917.json)。本任务结束，不继续追加优化或500题；旧模型成绩和资源测量保留原身份。

---

## 历史协作证据：有界存储单元四

两项缺陷经集中修复、自验及固定版本完整复审全部关闭；最终组合 `52797ade3d38c1dd4ce6a25436653734552a893265a9d9e0b7efcf3cc583dd94`，387项文件身份首尾一致。A的66项顶层测试、B的6项顶层及7项子测试通过；两轮审查、修复及验证材料保存于[单元四正式证据](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-unit-four-20260917.json)。原始资源与模型成绩保留各自版本身份。

---

## 历史协作证据：有界存储单元三

5项缺陷经集中修复、自验及固定版本完整复审全部关闭，无剩余阻塞项；最终composition为 `2e543a63555bfb235296273ede2d3de10c4b5526f04ad210c5780e64cae8e401`。345项源码身份前后一致，B独立执行的9项测试全部通过，两轮审查、修复和验证材料归入[单元三正式证据](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-unit-three-20260917.json)，原测量保留原版本身份。

---

## 历史协作证据：有界存储单元二

5项缺陷经集中修复、自验及固定版本完整复审全部关闭，无剩余阻塞项；最终composition为 `b7039c153806f4bbf44352f4a425238002ae27ad4d0ab60d23dd83fca8dee19c`。212项身份、三轮审查报告、修复与验证记录已归入[单元二正式证据](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-unit-two-20260916.json)及其复现包，原资源测量身份独立保留。

---

## 历史协作证据：有界存储单元一

单元一固定版本完整审查与集中修复后的独立复审已结束：RPC编号、特殊字符规范编码及批量回执逐项恢复问题均已关闭。验收composition为 `f43536c9af004820a8d8c8b46f5facd38e82fddf315654906f7d6e2d71d66ea7`；[修复、验证及完整审查结论](../../benchmarks/acceptance/suite/adapters/product_resource/bounded-unit-one-20260916.json)。

---

## 历史协作证据：存入时延优化

以下为2026-09-14至15日的已结束工作及可复用证据，不是本次待办。表内实验路径相对 `.tmp/ingestion-latency-collab-20260914/`；保留既有引用入口。

### 独立优化验收（2026-09-14）

| 独立方向 | 正式采用与收益 | 验证与边界；路径相对实验根目录 |
| --- | --- | --- |
| 1. 减少格式与引用错误 | 归一化唯一合法的单层位置／区间包装及对象分支，继续完整Schema校验；不修改事实、编号或证据选择。 | 回放6份输出消除15处格式错误，其中4份无需AI纠错即符合完整Schema；歧义、非法范围及错误来源不放行。`a/independent/normalization-audit.json`。示例虽出现局部提速，但改变部分证据覆盖，未采用。 |
| 2. 保留正确结果 | 中断后保留完整来源与有效纠错字段；后续事件被截断，或仅缺结束信号、外层括号及已识别的Markdown结束标记时无须重新生成，只补缺口。 | 146项检查及6份真实输出的402个截断边界回放通过；完整批次仅1次调用，格式纠错后中断不增加原有调用。内容未闭合、引用非法、字段冲突、模型错误和异常工具调用仍拒收；用量不完整标记保留。`a/independent/body-preservation/verification.json`；`a/independent/body-preservation/real-output-replay.json`。 |
| 3. 无损简化输出 | 保留原表示。顺序短号在重试子集可能串源；稳定短引用解决身份问题，但未取得足够综合收益，不采用。 | 实际5来源对照：原表示100.81秒、短引用119.95秒；短引用少一次纠错，但语义覆盖有取舍，保持原分组还增加准备工作。`a/independent/stable-refs-215516/`；`b/independent/stable-reference-boundary-review.json`。 |
| 4. 可靠复用 | 准备与评测续跑共用存入实现标识，包含提示拼装、分组预算及相关调用；答题／裁判设置不阻断复用。 | 相关实现变更会使准备失效，无关变更继续复用；旧准备文件不重写、不删除。不减少全新资料首次组织。`a/independent/final-gaps/verification.json`；正式 `test_prepare_materials.py`。 |
| 5. 等价并发与衔接 | 冷导入按原顺序发布已就绪批次，与后续生成重叠；涉及旧组织时保持原屏障。 | 50来源真实组织、关系、向量及原文取用一致；正式整合的冷／暖导入和失败恢复检查通过。固定服务耗时回放预计减少80.64秒，**不是新整批实测**。`a/ordered-pipeline-candidate/retained.json`；`b/independent/item5-integration/existing-checks.json`。 |
| 6. 传输开销 | 去掉HTTP请求JSON封装的冗余空白，提示、Schema及字段值不变；沿用已有提示缓存。 | 117份真实请求减少3739字节，收益很小，无新增调用。连接复用需要额外处理流尾，尚无足够收益证据，未引入。`a/independent/transport-byte-audit.json`。 |

正式实现的146项离线检查通过，包含中断恢复与缓存失效边界；[完整检查及自审结果](../../.tmp/ingestion-latency-collab-20260914/a/independent/body-preservation/verification.json)。自审覆盖响应事件逐字节截断、正文及Markdown包装逐字符截断、Unicode正文原样保留、格式纠错、逐来源校验及恢复后的调用次数，未发现此独立项范围内仍需修复的问题。这些改动不新增AI阶段、不改变原文、语义提示、模型档位及预算；不构成新的全套答题成绩或整批时效成绩。独立优化已收敛，组合优化不在此范围。

### 机制验证与可复用进展

| 机制 | 结果与下一步 | 证据（实验根目录下） |
| --- | --- | --- |
| A：同资料按自然批次集中交接 | 重复输入可减少约52%，但20来源合并请求约170秒，拆分关键路径约87秒；合并产物仅18／20通过组织校验，不扩大验证。尚非完整可用时间。 | `a/packing-audit.json`；`a/packing-first-pass-134357/` |
| B：以实际取用价值约束必要组织 | 3来源保住备份方法与更正的一项连接，但约75秒对69秒，输出增加，仅2／3通过组织校验；保留局部收益，不作为完整候选。 | `b/runs/necessary-connections-paired-1/` |
| 交接中丢失模型已选检索线索 | B发现、A复现并隔离修复：实际输出6／7／8条被正式交接各截为4条；候选完整保留且其他字段不变，离线验证通过。尚未采用，索引发布与取用效果仍待验证。 | `a/cue-handoff-verified/`；`a/preserve_cues.py` |
| A：原文检索片段加按需全文补读 | 首次输入缩小，但模型重新读了12份候选中的11份，7次HTTP、约227秒仍未完成；完整输入对照约122秒。因重复工作已超过对照而停止，未取得完整产物。 | `a/cold-context-135612/stop.json` |
| B：既有语义索引加按需全文补读 | 完整输入约153秒且通过组织校验；索引臂发生格式恢复及重试，5次HTTP，可观测耗时至少301秒，未完成。两种补读机制都增加了串行工作，不继续试同类措辞。 | `b/runs/index-context-paired-1/stop.json` |
| A：来源线索缩减候选的覆盖审计 | 445份来源中已发布43项跨来源引用；仅保留词法排序前4项覆盖35项，前8项覆盖40项。简单硬裁剪会漏已有关系，不采用。原关系并非完整标注，此项只用于排除裁剪方案。 | `a/source-cue-audit.json` |
| A：来源内组织先行 | 3来源不带旧资料生成检索入口和内部图，约71秒、1次HTTP，3／3通过组织校验；生成18个单元、17条关联，尚未处理跨来源关系已无时延收益，不扩批。 | `a/source-local-141205/` |
| A：单独生成来源检索入口 | 同3来源不带候选和图，约87秒、1次HTTP；仅入口生成也未提速，不扩批。文件时序显示约17秒在响应头前、70秒在流式响应期间，不能按输入字数预测耗时。 | `a/source-metadata-141551/`；`a/request-phase-audit.json` |
| B：关系端点内嵌单元，宿主生成ID | 3来源候选约137秒、2次HTTP，对照约54秒、1次HTTP；均通过组织校验。候选因外部对象提及不可表达而纠错，未证明提速；保留表示等价证据，不扩批。 | `b/runs/inline-first-pass-1/`；`b/inline-roundtrip.json` |
| A：同上下文提案的宿主交接 | 重放3份真实AI产物，通过正式MCP保存、领取工作、校验发布及导航；保留全部线索和关系，仅绑定实际身份。该本地交接约9.64秒，不含此前约71秒AI生成，不能冒充端到端提速；需验证真实智能体融合生成及旧资料关系覆盖。 | `a/proposal_handoff.py`；`a/proposal-handoff-142739/` |
| A：短信息与组织提案合并生成 | 一次真实AI响应生成原文和组织提案，经正式内核保存、校验并可检索，完整等待16.63秒。配对分支因原文封装不一致停止，不能宣称相对提速；未达5秒目标。 | `a/same-context-143254/` |
| B：短信息的模型时序 | 同90字符来源两次组织分别9.62、16.07秒，均1次请求且无纠错；响应头前约2.6秒，其余主要为生成。短请求也不能按固定调用耗时估算。 | `b/runs/short-source-timing-1/` |
| A：全量原文分段向量索引成本 | 50来源需要1308段，均匀抽取32段实际18.16秒；线性估算全量约742秒，仅为估算。未生成全量索引，不作为低成本替代方案。 | `a/raw-index-cost-143824/` |
| A：短来源已有检索能力 | 90字符空库写入约1.80秒；组织前后9种查询的命中、排序、依据一致，仅摘要末尾换行不同。仍处于待组织状态；该结果不能覆盖较长摘要、来源内部关系或后续增量关联。 | `a/short-ready-145015/` |
| B：固定批宽按候选重合分组 | 同20来源、相同模型配额与候选范围，对照145.75秒、组织校验19／20；重分组296.87秒、14／20，双方各4次HTTP。输入减少但输出和结构错误增加，不扩批；保留有效部分产物。 | `b/runs/overlap-grouping-paired-1/conclusion.json` |
| A：共享来源表示的依据覆盖 | 445来源的538条已有关系中490条位于来源内部。98个跨来源端点有94个可定位，仅50个完整依据包含于现有摘要和线索；不能仅削减跨来源比较，也不能直接以入口摘要替代完整关系依据。原关系不是正确性标注。 | `a/relation-representation-audit.json` |
| B：短来源完整证据入口 | 295字节来源的末尾条件原本未交付；补充全源证据引用后，经正式MCP完整交付，增加1次原文读取。其余3类控制保留原路径，core全包通过；无AI调用，组织仍待完成，不作为5秒达标证据。 | `b/short-evidence-candidate/retained-candidate.json` |
| A：按定义位置引用单元与提及 | 445份组织产物无损转换且正式校验全过，紧凑输出减少12.4%。实际候选132秒、2次HTTP、3／3格式有效，但最终未使用位置引用，且一条“不具备情感共情／能模仿共情”被误标矛盾；对照格式纠错到240秒仍不完整。不能据此归因提速或采用，不扩批。 | `a/positional-roundtrip.json`；`a/runs/positional-paired-resume-1517/conclusion.json` |
| A：原有格式修复成本审计 | 9份完成准备的117次HTTP中，29次为JSON字段格式修复，额外输入约259万Token、输出约5.14万Token；所有请求均未开启原生结构化输出。此成本不能直接换算可节省的完整等待，也不含真实关系判断修正。 | `a/format-cost-audit.json` |
| 原生结构化输出 | 实际复杂Schema约束会丢必填字段；等价展开Schema后仍丢6处角色信息，不采用。仅约束JSON对象的传输保住完整非空产物，保留为局部协议候选；它不能保证关系判断正确。 | `b/runs/native-schema-nonempty-branch-1/`；`b/runs/native-object-nonempty-branch-1/`；`a/runs/native-expanded-nonempty-1/conclusion.json` |
| B：JSON对象模式的真实取用 | 5来源完整等待约134秒，对照158秒，均找到3份必要资料；仍存在推导方向倒置和把相邻物品误判为组成关系。不能用资料覆盖通过代替关系质量通过，不扩批。 | `b/runs/native-object-navigation-1/` |
| B：补回既有关系定义 | 正式定义未完整交给模型属实；补回后仍到原240秒预算未完整交付，对照167秒也失败，不采用该候选。流现场显示：模型先识别关系，随后逐来源重复建图，反复将“关系涉及当前来源”误解为“当前来源必须是关系起点”。 | `b/runs/relation-definitions-navigation-1/` |
| A：批次统一关系清单 | 以全批连接为判断对象，一次报告后按明确来源归属分发；不由代码决定关系或答案。445份既有图、538条关系无损还原并通过原校验，传入关系方向保留；该离线结果只证明转换正确。 | `a/shared_connections.py`；`a/shared-connections-roundtrip.json` |
| A：统一清单真实对照 | 候选131秒、对照225秒，双方各2次请求，均未完整发布；候选少了对象提及所需的单元ID，格式修复仍失败。还存在不成立的组成关系，不按耗时较短记作提速成功，不扩批。 | `a/runs/shared-connections-navigation-1/conclusion.json` |
| B：超时后保全完整来源结果 | 用原始中断流穿过真实驱动和逐来源验证：原版保留0份，隔离修复保留3份有效产物；错误模型拒收，正常5份结果逐字不变。0新增AI，不修改超时或重试；尚未采用，不代表原任务已完成。 | `b/partial-source-recovery-candidate/retained-candidate.json` |
| 同批新对象的关系发布边界 | 冷库不能引用同批尚未发布的对象提及，促使模型反复改类型。先验证同批单元、再校验关系具有工程可行性，但完整发布还需处理依赖组、权限、修订及崩溃原子性；尚无时延收益证据，不据此直接扩大内核实现。 | `b/cold-batch-atomic-feasibility.json` |
| A：原生工具交付 | 精确交付完整非空图通过；真实5来源完整等待97秒，对照51秒，均1次调用、必要资料3／3，未证明提速。候选未重现对照中把同对象当整段同义、把相邻物品当组成的错误，保留这一局部质量观察和完整产物；尚未采用，不追加同类交付变体。 | `a/terminal-delivery-candidate/`；`a/runs/terminal-delivery-nonempty-1/`；`a/runs/terminal-delivery-navigation-1/` |
| A：证据节点与语义角色交接 | 从逐来源重复建图改为全批一次选取证据、连接角色，由宿主绑定身份。原型补齐上下文定位合同后约34秒、5／5结构有效；语义角色版约57秒，但同段多个对象的身份端点混淆。修复显式对象提及后约100秒、5／5结构有效，两个对象身份绑定正确；未实际发布，不代表完整可用时间。 | `a/runs/evidence-graph-context-contract-1/`；`a/runs/role-graph-feasibility-1/`；`a/runs/mention-role-graph-feasibility-1/` |
| B：未参与设计的条件例外检查 | 冻结语义角色接口，2份原文约30秒、1次请求，条件与例外完整、2／2结构有效，没有把雨天出租车例外误判为与默认地铁通勤冲突。支持局部质量收益，未测旧库发布和完整可用时间。 | `b/runs/role-conditions-independent-1/semantic-review.json` |
| A：对象绑定组合原生JSON对象交付 | 约79秒、2次请求仍失败：实际JSON增加第6份条目并生成无依据关系，纠错也未执行所要求的替换。原生对象约束不能视为可靠语义交付保障，停止该组合，保留无此约束的局部对象绑定成果。 | `a/runs/mention-role-native-object-1/` |
| B：长资料独立对照 | 原3份长资料约3.65万字，组合约85秒、对照91秒，均1次请求；没有足够整体收益。两臂的关键关联依据都漏了同段后半的兼容要求和临时期限；原文仍在，不能将结构校验通过当成完整条件交付。 | `b/runs/long-mention-role-native-paired-1/conclusion.json`；`condition-coverage.json` |
| 冷导入的生成与发布屏障 | 正式执行先等全部生成，再逐批校验、修复和发布；9份现场的提交尾部平均约270秒，其中修复调用约150秒，其余约120秒不能全部归为向量耗时。A隔离实现下一就绪自然批先处理、发布仍保持原顺序；已有检查及提前提交、失败接续、含旧快照回退均通过，未修改正式源码。 | `a/publication-barrier-audit.json`；`a/ordered-pipeline-candidate/`；`b/publication-tail-attribution.json` |
| 50来源真实流水线 | 约49万字符原文写入2.52秒，8生成单元均完成；首次提交在144.70秒、末次生成在224.81秒，发生约80秒重叠。监督误停时39来源已提交，余项未完成；离线正式结构检查49／50有效，不能计完整时效。原始输入和提示词身份经B独立核验。 | `a/runs/cold50-ordered-pipeline-1/`；`b/live-pipeline-cross-review.json` |
| B：本地向量批量端口 | 同16片段、原模型与2线程，逐条5.18秒、现有批量接口4.17秒，向量逐值相同。只证明局部端口能力；原总320字节限制兼容较小窗口提供方，不能直接放宽，也不外推整批省时比例。 | `b/runs/vector-tail-actual-1/result.json` |
| A：原结构校验与修复提前执行 | 50份初始生成结果中48份有效，批量检查约1.4秒；此前49份有效包含了提交阶段修复，两者口径不同。新接入独立执行原提交修复预算，回放保住3份成功结果、修复后完整内容及接续；预检回执不能代替发布，真实版本变化直接报错。将结构检查混入格式修复预算的初稿不采用。 | `a/early-validation-replay.json`；`a/preflight-integration-checks.json` |
| A：真实单来源恢复 | 复用所属9份生成结果，仅重做Cita一份；1次模型调用、45.52秒，9份均通过正式结构合同。模型补充了对象所在原文作为必要上下文，保留2个单元和4条关系；这是有效局部恢复，不是新的50来源完整验收或已证实的提速比例。 | `a/runs/preflight-recovery-1/` |
| A：保存产物的真实补发布 | 在原停止现场的独立副本中，复用全部生成结果与Cita修复，11份真实发布约30.61秒，最终50份均为ready；无新增AI，原现场校验值未变。只证明产物能够真正落库，不能把跨中断时间拼成完整等待，也不代表50份关系全部正确。 | `a/runs/saved-publication-completion-1/result.json` |
| A：向量批量的顺序平衡复核 | 同32份片段、同一运行实例，批量／逐条／逐条／批量平均为9.82／9.37秒，向量逐值相同。未复现16份试验的提速，不采用声明批量能力的隔离修改，不再扩大该路线。 | `a/runs/vector-batch-abba-1/result.json` |
| B：直接定位原文对象 | 原模型单次生成正确的同对象关系，约90.70秒；对照118.24秒、3次请求后仍未建立该身份关系。经正式发布后，两端原文和同对象导航均有效，13份输入逐字一致；旧功能及过期、重启检查通过。支持局部质量与生成成本收益，完整等待含人工检查和入口纠正，不能宣称整批提速。 | `b/source-object-candidate/retained-candidate.json`；`b/runs/source-object-max-paired-1/conclusion.json` |
| A＋B：执行与对象端点组合接入 | 实际模型产物经新校验器、原提交内容生成和缓存接续均通过；原校验器确实不能识别新协议，已隔离编译匹配版本。没有丢失对象字段或扩展纠错次数；0新增AI，仅证明组合接口一致。 | `a/combined-candidate-checks.json` |
| A＋B：完整冷导入及独立取用 | 50来源完整等待319.11秒，其中写入1.94秒、组织发布315.49秒；12次外部调用实际产生17次模型请求，含5次格式纠正。50原文及6来源29次证据读取一致，3个预选场景保住条件与更正；Babylon的公司／产品身份仍有疑点，工具依据也有选段不足。该配置未完成整体质量确认，不能与共享负载下714秒直接比较。 | `a/runs/combined-cold50-2/result.json`；`output-cost-audit.json`；`b/combined-cold50-semantic-review.json` |
| 组合实验的实际能力身份 | 17个实际请求均缺少对象端点候选的新增说明：创建任务上下文时基类替换了候选子类。已在隔离入口绑定上下文，并通过完整准备入口的0AI检查；319秒记录仅代表实际混合配置，独立Max对照不受影响。 | `a/runs/combined-cold50-2/effective-capability-audit.json`；`a/scope-binding-check.json` |
| A：纯按序发布的50来源等价验证 | 两个真实库的50份最终提交、模型初始及修复输入输出、归一化组织内容和向量逐值一致；各50次原文读取一致，各3个导航入口可用。使用相同本地成本和固定模型服务耗时，排程区间由397.87秒推算为317.24秒，预计减少80.64秒；不含新增模型实测，不能替代完整等待成绩。B已复核算式、8槽条件和证据边界。 | `a/ordered-pipeline-candidate/retained.json`；`a/runs/ordered-publication-replay-2/quality-equivalence.json`、`controlled-timing-comparison.json`；`b/ordered-pipeline-retained-cross-review.json` |
| B：精确关系依据附带完整原文上下文 | 复用50来源的112条关系，主端点、含义、原有上下文及图和向量数据不变；12条关系补14条上下文引用，12次去重读取均与原文一致，core回归通过。局部读取检查由3.06秒增至4.05秒，包含额外读取；属于完整性收益及成本，不计作存入提速，也不代表关系判断全部正确。 | `b/graph-line-context-candidate/retained-candidate.json`；`b/runs/graph-line-context-public-1/result.json` |

语义质量按原文、对象身份、条件和关系含义核验，允许不同但忠实的表示；类别选择存在规范歧义时先核对真实取用影响，不将术语差异直接记为事实错误，也不以模型思考中的正确判断代替实际交付。

监督要求：在单次尝试结束和重试开始的边界检查具体失败，缺少新依据的整项重试不继续；实验结束即登记结果与资源状态，不能把选路中的停留记作运行进展。
停止运行前须核对当前工作单元的最终状态、已完成恢复和在途请求；历史尝试失败不能代替当前失败，局部恢复也不能误记为整项重跑。


## 语义组织组合优化（2026-09-15）

本环节采用标准见[第4子环节](../tasks/task-quality-improvement.md#第4子环节可优化方向)：比较错误、有效关系覆盖及完整成本；历史实验的“未采用”不等于没有局部收益，不能仅因仍有错误排除候选。

关系复审核对类型、方向、两端原文、条件及实际交付的单元上下文；不能只截取端点判断依据缺失，也不能用关系条数代替有效关联覆盖。

**采用原文对象端点与单次调用内的关系分工，保留完整材料、请求内去重及既有分批预算。** `same_object` 无需引用尚未发布的外部单元；内部关系和跨来源关系分别交付，代码只校验、映射和合并容器。原模型、档位、原文、候选和预算不变，不增加AI阶段。

| 机制验证 | 结果与取舍 |
| --- | --- |
| 直接对象端点，但未明确内部关系任务 | 短资料改善，长资料出现零关系；速度不能抵消条件遗漏，不采用。 |
| 重写语义任务、扩大端点表示选择 | 短资料变快，长资料返工增加并出现错误冲突；不采用。 |
| 原文对象端点＋内部／跨来源分工 | 四组11份来源均完成，每组一次调用；正式入口复放保持真实AI产物不变。实现已采用，后续复审发现关系含义与端点不符、错误等价等问题，质量验收未关闭。 |
| 上述方案再补充完整关系类型定义 | 两组约145／154秒，还混淆同类标识与同一对象；两请求完成后结束此路线，不采用。 |
| 合批、按材料重叠分组、裁剪候选 | 复用前述已有对照：分别出现耗时上升、有效结果减少或关系覆盖下降；保留原供给与分批方式。 |

| 同条件语义生成 | 原方式 | 采用方式 |
| --- | ---: | ---: |
| 五份短来源关系链 | 86.8秒 | 100.0秒 |
| 三份长来源内部关系 | 94.1秒 | 57.5秒 |
| 一份来源＋12份对象候选 | 80.3秒 | 51.0秒 |
| 两份通勤条件资料 | 38.8秒 | 46.0秒 |
| **平均每组等待** | **75.0秒** | **63.6秒** |
| 累计模型请求 | 4次 | 4次 |
| 累计输入Token（含缓存读写） | 76,015 | 83,373 |
| 累计输出Token | 16,360 | 14,024 |

平均生成等待减少15.1%，输出减少14.3%，输入增加9.7%；两个较小组更慢。费用需结合输入、输出及缓存计价，不能直接宣称下降。以上为四组配对生成的均值，不是单条存入或约49段对话整批可用时间，历史714秒不据此改写。

验证覆盖：11份真实生成结果均通过正式Python入口和Go内核；5份来源完成正式MCP发布、导航、32次证据读取且原文一致；单元测试覆盖旧v1与新v2共同恢复、过期来源拒绝、名称归属、原文条件、总关系预算、局部返工及有效结果复用。152项Python测试及6个相关Go包测试通过。结构通过不等于所有模型关系判断必然正确；质量结论限定于已核对场景，未新增完整14／24题成绩。

11份归一化关系记录的紧凑JSON由49,181字节变为84,852字节，主要因长资料保留了更多证据与上下文；这是关系载荷而非整个存储目录大小。该量级未构成可见取用负担，不据此外推大库成本。

后续复审撤回原“未发现缺陷、停止优化”的结论：结构、兼容及原文一致性验证仍有效，不能替代关系含义与实际依据的核对。更大规模导入的实际等待与费用须在相应准备任务中测量，不以局部比例代替。

证据与恢复：[完整验证与版本清单](../../.tmp/ingestion-combined-20260914/retained/manifest.json)、[原始对照](../../.tmp/ingestion-combined-20260914/paired-4/summary.json)、[补充场景](../../.tmp/ingestion-combined-20260914/holdout-1/summary.json)、[正式输入一致性](../../.tmp/ingestion-combined-20260914/formal-request-parity.json)、[正式产物复放](../../.tmp/ingestion-combined-20260914/formal-replay.json)、[公开取用验证](../../.tmp/ingestion-combined-20260914/formal-publication-review.json)。最优实现已进入工作区正式源码；隔离产物与恢复文件保留，未提交。

### 后续修复与自审（2026-09-15）

| 问题与处理 | 验证结论 |
| --- | --- |
| 原文定位截断一行，取用时缺少同句的限定信息 | 已修复：附带完整边界行，保留原端点、类型、含义和条件；没有新增AI、原文副本或索引。3份真实长资料的15条关系保持不变，补3处上下文引用，全部33次读取与原文一致；局部导航约0.0157秒／0.0182秒，不代表完整任务耗时。 |
| 关系判断与端点不一致 | 未解决：旧产物把狗粮信息连到驱虫药费用条目，把不同工具清单的重叠写成等价。确定性映射并未改错模型输出；新增实验未证明稳定修复，正式提示和语义表示未替换。 |
| 说明、引用和角色接口实验 | 简洁关系定义、原文引文、带内容的引用编号、具名关系角色及组合均有局部改善，但仍有错连、方向问题或大量返工，未采用。早期引文试验遗漏替换旧数字定位说明，不能据其失败判断引文机制无效；修正完整输入后的试验也未取得合格整体结果。 |

已采用的上下文修复通过Unicode／换行边界、公开导航、过期原文拒绝及6个相关Go包验证。它只补齐已选片段所在行，不会自动发现模型未选择的其他依据，也不修正模型的语义判断；最终取舍见下方收敛结论。

本阶段运行已停止，未改变暂存区；保留先前可恢复实现与新增无损修复，未用失败实验替换正式语义方案。[修复、自审与接续证据](../../.tmp/ingestion-combined-repair-20260915/review.json)、[公开取用对照](../../.tmp/ingestion-combined-repair-20260915/context-fix/public-check/result.json)。

### 等价边界与推导方向验证（2026-09-15）

固定模型、档位、原文、候选与预算，比较四组共11份来源：两组历史对话、两组既有关系与条件样例。结构校验、关系含义、有效关联覆盖和完整生成成本分别核对，关系条数不作为覆盖率。

| 机制验证 | 已观察收益与采用结论 |
| --- | --- |
| 在关系类型字段说明等价边界及推导方向 | 格式与引用校验由对照9／11变为11／11，平均每组生成74.67秒降至56.00秒；但仍错误合并部分重叠的信息，并遗漏对照中的日期澄清关联。保留局部收益及完整产物，未采用。 |
| 加强完整含义与依据范围的约束 | 纠正了通勤样例的错误等价；配对复验却遗漏长资料中的多项有用关联，不能只保留首轮成绩。未采用。 |
| 简化约束、补示例或改变引用表达 | 有局部纠错，但未同时保住含义、引用与覆盖；最简定义试验新增无依据的对象关联，长资料首次请求超过240秒进入补做，已停止。未替换正式实现。 |

**未采用的依据是新增错误、有效关联遗漏或成本问题，不是仍有偶发错误。** 两条约束能改善局部判断，尚未证明整体语义质量提升；也未据此得出总体错误率。保留的已知短板包括：模型描述了某个真实联系，却选中了不能支持它的原文位置。

正式实现保留已验证的关系接口、任务分工及原文上下文修复；12份相关代码文件与既有恢复清单逐一校验一致，实验均未进入正式代码。各方案、对照、局部收益、失败及恢复位置见[约束验证记录](../../.tmp/ingestion-relation-constraints-20260915/review.json)。

### 关联定位与分类的接续验证（2026-09-15）

| 机制 | 收益与限制 |
| --- | --- |
| 用原文引文定位，或声明一次依据供关系引用 | 部分场景的依据更准确；整句抄录、引用编号及作用域错误引发返工，未形成可采用的整体改善。仅用段号又无法区分同段的不同陈述；短原文锚点可表达这种区别，但仍有有用关联遗漏。 |
| 明确关系两端的角色与类型名称 | 改善了方向判断；长资料中却减少了独立有用的关联。补充覆盖提醒未消除该问题，不能用少生成代替质量提高。 |
| 在同次响应中先定位关联、后分类 | 具名角色与两端方向分离仍有错误等价、推导或关联遗漏，并出现返工；没有取得可采用的整体改善。 |
| 固定已找到的依据，独立判断关系 | 能修正部分方向，但仍有错误冲突及推导，每组新增40—129秒；不追加该AI环节。 |
| 明确证据导航目标 | 保留原文与一次调用方式，仍把一般习惯当作具体时长的依据，并产生错误冲突；不采用。 |
| 完整输入输出示例 | 四组11份来源均通过结构校验，通勤关系不再标为完全等价，部分推导方向改善；长资料仍遗漏有用联系，并把问题之间的先后联系标成支持。平均每组80.53秒，对照74.67秒，累计输入增加；不采用。 |

各机制的完整输入、原始响应、质量核对及运行记录见[验证记录](../../.tmp/ingestion-relation-constraints-20260915/review.json)，未采用的实验不进入正式版。

### 组合优化最终收敛（2026-09-15）

**7—9组合优化已稳定，不再投入本项。** 第7项已采用原文对象直接定位与完整原文行上下文，第9项已采用单次调用内的内部／跨来源关系分工；第8项保留原材料供给与分批方式，没有新增优化。后续提示、表示及分类实验未取得可采用的整体替代，均未进入正式版；已登记的语义短板保留，不宣称消除全部模型错误。

| 最终取舍 | 依据与边界 |
| --- | --- |
| 保留已有改善 | 原接口与分工已完成正式接入、原文取用和兼容验证；上下文修复无新增AI，保持原关系与原文，补齐同一行的限定信息。四组75.0→63.6秒为先前配对生成记录，不是新整体测速。 |
| 不采纳后续实验 | 有局部纠错，但同时发生有效关联遗漏、新增错误或不可接受的额外成本；完整示例也未形成整体优势。没有用仍有错误这一点单独否决改善。 |
| 保留已知语义短板 | 错误等价、方向和依据定位可能误导关联解释、排序或按类型导航；不会由内核自动纠正，也不能承诺外部智能每次都纠正。 |
| 发布影响 | 定向验证故意提交错误等价和推导关系：两端原文及身份不变、关系标为派生解释、具体时长与天气条件仍可检索读取，更正后旧关系失效。无证据表明这些错误阻塞基本保存与取用，可带此短板进入发布验收；这不是整体质量或总体错误率达标的证明。 |

最终相关源码与两份保留清单的12个文件逐一核对；本轮未替换正式代码、模型或提示词，未改变暂存区。恢复时在隔离检出中依次应用[已采用实现](../../.tmp/ingestion-combined-20260914/retained/manifest.json)与[上下文修复](../../.tmp/ingestion-combined-repair-20260915/retained-context-fix/manifest.json)，以[最终收敛清单](../../.tmp/ingestion-relation-constraints-20260915/final-boundary/closure.json)的源码哈希和本节结论为准。历史清单中“无剩余缺陷”的旧结论不再适用。

### 纠错上下文优化（2026-09-15）

**第10项已稳定，已进入正式源码。** 无工具的语义组织首次字段纠错不再发送上一轮已完成的推理，完整输入、候选、已有输出及校验均保留；带工具的取证、其他任务和不能定位字段的纠错不变。已接受结果继续复用，不追加AI阶段。

固定三组真实失败现场的原始输入与首轮响应，使用相同模型、档位及纠错预算，仅比较下一次纠错请求；共6次真实调用，最多3路并发。

| 纠错场景 | 原方式 | 采用方式 | 输入Token（原→新，含缓存） |
| --- | --- | --- | ---: |
| 单来源的线索字段超长 | 完成，28.13秒 | 完成，22.41秒 | 56,648→55,217 |
| 长资料的线索字段超长 | 完成，18.99秒 | 完成，21.13秒 | 15,661→12,880 |
| 长资料中缺失两条完整分析 | 未按要求补交，55.99秒 | 完成，74.61秒 | 24,522→19,302 |

输入合计减少9.7%，输出合计增加8.1%，其中新方式补完了对照未完成的分析；不能据此宣称整体费用或平均等待下降。前两组正常纠错平均23.56→21.77秒；第三组失败与完成的耗时不能作为同质量提速对照。以上不是整批导入计时，也不是总体错误率统计。

质量核对：前两组仅改超长分类字段，其他内容及关系原样保留；第三组保留已有分析，补交的13条关系与历史同现场成功产物的类型、两端原文及条件一致，未发现有用联系丢失。正式解码及内核校验通过；既有关系含义短板未由此消除。135项相关测试覆盖完整校验、越界修改拒绝、中断保留、工具历史及准备复用边界。

**到此结束该项优化。** 继续删减原文、候选或已有结果需要改变语义依赖，缺乏可证明无损的进一步裁剪依据；不以减少材料或调用失败换取表面速度。实现、请求、响应、核对及恢复说明见[保留清单](../../.tmp/repair-context-20260915/retained/manifest.json)。
