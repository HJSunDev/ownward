# 开发工作台

## 一、用户提示词

### 审查工作留痕体系

```text
以乔布斯式产品直觉和极致删减标准，重新审查这套工作留痕体系。

颗粒度是审查核心：体系不是工作日志，不得记录日常操作、实现细节和可由代码或结果还原的过程，只保留真正影响后续工作的关键进展、关键决策与选择、关键问题及解决方案。任何不能帮助后续智能体接续工作、作出判断或避免重大重复错误的内容，都不应进入体系。

检查现有设计是否会造成过度记录、内容重复、无限膨胀或额外维护负担，并提出最小、直接的优化方案。所有规则与记录必须凝练、简洁、清晰，禁止长篇论述、重复解释和低价值信息。

先给出审查结论及需要优化的内容，不要直接修改。
```

### 持续收敛架构总纲

```text
目标：持续审查并收敛 `overview.md`，直到它准确承载产品需求，并成为当前范围内最简单、完整、能够经受长期变化且足以支撑预期效果的架构总纲。

`goal.md` 已封版；`requirements.md` 是产品权威；`mragent-paper-research.md` 只提供研究依据，不构成必须照搬的实现方案。只允许修改架构总纲，不得修改其他文档。现有内容能够成立的应当保留，不以大改为目标。

每次开始、续跑或上下文压缩后，重新完整读取上述文档的最新版本，不依赖旧结论。

持续执行以下循环：

1. 先从目标与产品需求独立推导架构必须回答的问题，再审查现有架构总纲是否准确承载。
2. 以“首席产品官 + 乔布斯式产品直觉 + 顶级架构师”的标准判断：
   - 是否抓住产品不可替代的本质与边界；
   - 是否形成职责清晰、依赖稳定、逻辑闭合的整体架构；
   - 是否以最少的结构完整承载需求，没有多余能力、重复职责和低价值复杂度；
   - 当模型、存储、检索技术、协议和智能个体形态变化后，核心架构是否仍然成立；
   - 是否不存在阻碍检索时效、检索质量、信息组织质量以及轻量运行目标的结构性瓶颈。
3. 严格控制颗粒度。架构总纲只保留整体结构、核心职责、能力边界、依赖方向、关键不变量及质量目标的架构响应；不得进入模块拆分、接口定义、数据结构、算法流程、技术选型、参数指标、实施步骤和交付计划。
4. 每项架构判断必须能够直接反绑产品需求，或属于满足明确需求不可缺少的架构推论。不得以最佳实践、未来可能、技术先进性或“更加完整”为理由扩张范围。
5. 完整审查一轮后统一归并问题并集中修复根因，随后复审受影响范围。最优架构不是抽象最多或预建最远，而是锁定范围内最简单、完整、清晰、可演进且没有已知结构债务的方案。
6. 准备结束时，对同一份未修改的最新文档分别执行产品本质、整体架构、长期稳定性与性能支撑、需求反绑与范围控制四次独立冷启动审查，不得复用此前结论。任何一次发现问题，都应继续修复并重新执行全部审查。

只有产品依据本身存在冲突或缺失，且不同选择会改变产品定位或需求范围时才暂停询问；其余架构判断自主收敛。

完成条件：

- 所有必须由架构承载的需求均有准确落点；
- 产品边界、核心职责、依赖关系和关键不变量清晰一致；
- 架构能够支撑既定质量与性能预期，不存在已知结构性阻碍；
- 内容始终停留在架构总纲层级，没有设计细节和实现细节；
- 不依赖当前具体技术，也没有为未来预建产品能力；
- 不存在需求之外的复杂度和已知结构债务；
- 同一份未修改文档通过全部冷启动审查。

满足后回复“架构总纲收敛完成”，简要说明最终判断和实际修改，立即停止，不再追加视角、继续润色或扩大范围。
```

### 持续收敛第一版完成条件

```text
目标：持续审查并收敛 `first-version-delivery-definition.md`，直到它成为第一版必要、充分且可执行的完成边界：全部条件通过就能确认产品真正完成，任何一项未通过都不能结束开发。

`goal.md` 已封版；`requirements.md` 是产品权威；`overview.md` 规定架构方向；`development-collaboration-guidelines.md` 规定代码质量。只允许修改第一版交付定义，不得修改其他文档。

每次开始、续跑或上下文压缩后，重新完整读取上述文档的最新版本，不依赖旧结论。

持续执行以下循环：

1. 先从目标、需求、架构和开发规范独立推导第一版必须交付的最小完整产品及其质量要求，再审查现有完成条件是否准确覆盖。
2. 以“首席产品官 + 乔布斯式产品直觉 + 顶级架构师 + 顶级性能工程师”的标准判断：
   - 所有条件通过后，产品本质、完整使用闭环和架构不变量是否真的成立；
   - 每项条件是否具有明确对象、固定前提、可观察结果和通过标准，使独立第三方无需猜测即可判定；
   - 是否存在条件全部通过但产品实际上没有完成的假通过，或产品已经满足需求却被需求之外条件阻断的假失败；
   - 是否能够防止通过模拟实现、人工补偿、旁路能力、特制数据、缩小范围、挑选结果或临时调参取巧达标；
   - 产品、代码和项目整体是否达到顶级质量，不存在边界内已知缺陷、架构偏离或结构债务；
   - 检索时效、检索质量、信息组织质量，以及程序体积、CPU、内存、存储占用和写入、持久化、组织、索引、检索效率，是否在等价条件下达到需求规定的同类前沿，且没有以牺牲其他目标换取单项达标。
3. 严格控制颗粒度。本文只规定第一版交付边界、验收基线、完成条件、明确排除和最终判定；不得进入实现方案、模块设计、技术选型、接口细节、任务拆解和具体执行命令。
4. 每项条件必须能够反绑目标、产品需求、架构约束、开发规范，或属于证明产品完成不可缺少的验收推论。无法证明必要性的内容删除，不得以最佳实践、未来可能或“更加完整”扩大第一版。
5. 完整审查一轮后统一归并问题并集中修复。修复后复审受影响范围，确保所有条件共同覆盖同一份产品，而不是各自证明互不相干的局部能力。
6. 准备结束时，对同一份未修改文档执行四次独立冷启动审查：
   - 产品完成性：全部通过后是否已经形成可实际使用的最小完整产品；
   - 判定清晰性：独立验收者能否无歧义地判断通过或失败；
   - 质量与性能：能否以可复现证据证明顶级质量和全部性能目标；
   - 范围控制：是否遗漏必要条件，或混入需求之外的能力与实现细节。
   任何一次发现问题，都应继续修复并重新执行全部审查。

只有产品依据本身存在冲突或缺失，且不同选择会改变第一版产品范围时才暂停询问；其余验收设计自主收敛。

完成条件：

- 第一版边界最小但完整，全部产品本质和使用闭环均可真实验收；
- 每项条件必要、可观察、可复现并能明确判定；
- 不存在假通过、假失败、取巧路径、主观措辞或待定标准；
- 所有质量与性能目标都有不可规避的验收落点；
- 完成判定绑定同一份未修改产品、固定基线和完整原始证据；
- 不包含需求之外的能力、未来预建或实现细节；
- 同一份未修改文档通过全部冷启动审查。

满足后回复“第一版完成条件收敛完成”，简要说明最终判断和实际修改，立即停止，不再追加视角、继续润色或扩大范围。
```

### 持续收敛语义组织边界与验收体系

```text
目标：持续审查并收敛本次语义组织问题引发的文档修改，直到它们准确表达 Ownward 的产品预期、形成能够经受长期变化的架构边界，并足以作为重新进入持续开发的稳定依据。

`docs/delivery/goal.md` 已封版，`docs/product/requirements.md` 是唯一产品权威，只能读取，不得修改。允许修改：

- `docs/architecture/overview.md`
- `docs/engineering/development-collaboration-guidelines.md`
- `docs/delivery/first-version-delivery-definition.md`
- `docs/research/first-version-benchmark-basis.md`
- `docs/delivery/acceptance-system.md`
- `benchmarks/acceptance/suite/README.md`
- `benchmarks/acceptance/suite/contract.json`

不得修改代码、产品依据和既有工作记录，不以大改为目标，现有内容能够成立的应当保留。

以下预期是审查判断标准，不要求在文档中逐字复述：

Ownward 以最低必要复杂度长期承载用户的信息资产，借助可替换、持续进步的通用智能能力理解信息，由内核将理解转化为可靠、可演进的组织与检索能力；在不牺牲功能完整性、信息质量和资产耐久性的前提下，使信息组织、检索质量、检索时效与资源效率同时达到同类前沿，让任何外部智能体都能低成本使用同一套持续成长的个人信息体系。

每次开始、续跑或上下文压缩后，重新完整读取全部相关文档的最新版本，不依赖旧结论。

持续执行以下循环：

1. 从目标和产品需求重新推导本次问题真正需要修复的最小内容：
   - 涉及具体内容含义的理解由可替换的外部语义能力承担；
   - Ownward 内核负责将语义理解转化为可靠、可演进的组织与检索能力，并对最终质量负责；
   - 确定性逻辑负责资产、身份、版本、来源、证据、明确语义、冲突、派生状态和结构完整性，不替代语义能力判断开放内容真伪；
   - 通用语义能力不能靠不断增加领域、词语、句式或验收样本规则实现；
   - 固定内核基线证明长期不变量，固定 Ownward 专项数据验证产品能力，LongMemEval‑V2 证明公开质量与时效前沿。
2. 以“首席产品官 + 乔布斯式产品直觉 + 顶级架构师”的标准审查：
   - 是否在不牺牲功能完整性、信息质量和资产耐久性的前提下，同时支撑信息组织、检索质量、检索时效与资源效率达到同类前沿；
   - 是否把稳定基础设施与持续进步的智能能力放在正确边界；
   - 是否没有引入内置智能体、额外产品能力或需求之外的复杂体系；
   - 当模型、检索技术、信息领域和表达方式变化后，核心判断是否仍然成立；
   - 各文档是否只承担自己的职责，没有重复解释、互相矛盾和越级内容。
3. 极致删减。删除为了回应修改过程而出现的解释、口号、重复原则、任意数字和低价值规则；只保留会约束架构、开发或完成判定的内容。删减只消除无效复杂度和资源浪费，不得降低产品质量、性能标准或删除必要架构能力。
4. 统一修复全部受影响文档：
   - 架构总纲只保留长期职责、能力边界和不变量，不进入模块、算法和技术细节；
   - 开发规范只保留持续编码必须遵守的简洁规则；
   - 第一版交付定义必须给出必要、充分、可明确判断的完成边界；
   - 基线依据和验收说明必须把固定内核、固定专项和公开前沿的职责说清，并能直接指导后续验收实现；
   - v1～v5 必须始终只是历史回归基线，不能被重新描述为第一版完成证明；
   - 当前最终验收能力存在缺口时必须明确呈现，不得让文档产生已经可以完成判定的假象。
5. 固定专项数据、真值、评分、资格子集、候选绑定和失效规则必须足够清晰，使后续开发者无需重新解释即可执行；不得在候选冻结后生成验收数据或用无依据的规模制造虚假严谨。
6. 完整修复后，对同一组未修改文档分别执行产品本质、架构长期性、验收有效性、文档职责与范围控制四次独立冷启动审查。任何一次发现问题，都继续修复并重新执行全部审查。

只有产品依据自身存在冲突或缺失，且不同选择会改变产品定位或第一版范围时才暂停询问；其余文档判断自主收敛。

完成条件：

- 文档准确表达真正的产品预期，没有把实现问题扩大成新的产品需求；
- 可替换的外部语义能力负责开放内容理解，Ownward 内核负责资产、组织与检索；长期角色不绑定某个智能体或模型，第一版由接入的外部智能体兼任，责任边界清晰、克制且经得起技术变化；
- 不存在依靠特定内容规则实现通用语义组织的架构空间；
- 固定内核、固定专项和公开前沿三层验收职责清晰，完成判定不存在假通过；
- 每份文档只包含自身职责内不可缺少的内容；
- 没有低价值解释、重复表述、任意约束和需求外复杂度；
- 独立智能体仅依据这些文档即可重新进入持续开发，无需猜测产品方向、架构边界或结束条件；
- 同一组未修改文档通过全部冷启动审查。

满足后回复“语义组织边界与验收体系收敛完成”，简要说明最终判断和实际修改，立即停止，不再继续润色、追加视角或扩大范围。
```

### 审查自主开发治理闭环

```text
你再次以乔布斯的身份，以这个外人的视角，再次看一下我们这个体系的闭环核心，看以下几个问题：

1. 是否真的有效：能否符合我们预期的去工作，能否真正的去解决我们面临的主 Agent 钻牛角尖的现状。子 Agent 它就得从宏观视角去看待这个任务进度了或者得发现主 Agent 进入效率低下问题并指出问题和方案，对吧？所以这个是很重要的，是否有效。
2. 外人用这个技能能否独立的建立起这么一套体系。
3. 有没有那种看起来有用，实际上会干扰整个效果的类似问题。
4. 从子 Agent 的触发视角，可能在多个场景，比如刚开始开发，任务正常进行中，任务低效率进行中，任务快执行完等时机都会触发，不同场景触发子 Agent 在现在的约束下能否正常工作，能否符合预期工作。

补充信息：你压缩的频率挺高的，不需要担心长时间没压缩的猜测问题，不存在。

有问题给我报告，都满足直接简洁回复，不要硬找。
```

### 持续完成第一版开发

```text
目标：持续完成 Ownward 第一版，直到 `docs/delivery/first-version-delivery-definition.md` 的全部完成条件在同一最终版本上真实通过。首次启动或中断后恢复均以仓库当前事实为准，从最近的有效检查点继续。

工作依据：

- 产品目标与需求：`docs/delivery/goal.md`、`docs/product/requirements.md`
- 整体与模块架构：`docs/architecture/overview.md`、`docs/modules/storage/architecture.md`、`docs/modules/embedding/README.md`、`docs/modules/semantics/README.md`、`docs/modules/retrieval/README.md`
- 已确定方案：`docs/research/vector-model-selection.md`
- 工程规则：`docs/engineering/development-collaboration-guidelines.md`、`docs/engineering/commit-guidelines.md`
- 完成与验收：`docs/delivery/first-version-delivery-definition.md`、`docs/delivery/acceptance-system.md`、`docs/research/first-version-benchmark-basis.md`、`benchmarks/acceptance/suite/README.md`、`benchmarks/acceptance/suite/contract.json`、`benchmarks/longmemeval_v2/README.md`
- 关键留痕：`docs/records/README.md` 及其要求读取的当前分卷和直接引用记录

首次启动、暂停后恢复、重新创建或上下文压缩后：

1. 重新完整读取上述依据的最新版本，不依赖旧上下文转述。
2. 检查 Git 状态与历史、仓库结构、现有代码、测试、构建、验收材料和有效留痕，区分已经成立、正在进行、已经失效和尚未开始的工作。
3. 保留并理解现有变更；依据事实重建当前阶段、已满足与未满足的完成条件、最近有效检查点和下一项最关键工作。依据变化时只失效受影响的旧结论和证据，不重复仍然成立的工作。

恢复后报告一次整体完成进度；此后只在实质阶段完成时更新，不以局部操作冒充整体进度。

从最关键的未满足条件反推下一完整工作阶段，按工作依据持续完成实现、验证、验收和必要留痕。普通技术与执行问题自主确认、修复或换路后继续；只有需要用户决定的产品问题，或客观证据证明继续当前投入会产生异常成本时才暂停。

允许在本任务范围内按提交规范执行暂存和提交，不执行 `git push`。

只有同一最终候选及完整证据真实满足全部完成条件，所有应提交变更均已形成合格提交，且工作区不存在本任务遗留变更时才能结束；否则继续推进。满足后回复“第一版持续开发完成”，简要说明最终成果、验收证据、关键判断和提交结果，立即停止。
```
