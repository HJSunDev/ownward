# 第一版验收

当前固定基线是 `v5/`。`v1/` 因把信息类型放入输入而泄漏组织答案，`v2/` 又遗漏类型金标的通过阈值，`v3/` 未覆盖信息更新后的增量一致性，`v4/` 要求在无图召回已经饱和时仍取得正召回增益，均永久保留但不得用于完成判定。v5 将无效的原始召回增益改为正确关系证据覆盖增益，数值标准不降低；其他内容不变。

固定数据全部是为验收编写的合成信息，不包含用户的真实个人信息；它只覆盖产品规定的信息范围、关系和场景，不向实现提供金标答案。
