# 固定内核材料

`dataset.json` 用于隔离观察 Ownward 内核的身份、组织、索引和检索行为，不评价向量模型自身的语义质量。

文档向量是版本内固定的 16 维数值输入；查询向量是对应查询全部期望文档向量的算术平均值再归一化所得，并与文档向量一同冻结。观察器只读取这些数值，不调用模型或外部智能，因此候选之间的差异来自内核实现，而不是模型波动。

固定随机种子、规模和变体共同改变身份、顺序、时间、上下文与干扰项。材料内容及摘要由 `materials/manifest.json` 绑定；改变材料必须形成新的材料版本，不得覆盖本版本。
