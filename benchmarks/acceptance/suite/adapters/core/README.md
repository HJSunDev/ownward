# 固定内核层适配

本适配由 Acceptance Suite 唯一入口调度并接收报告，不是独立完成轨道。它只通过最终发布二进制的 CLI 与 MCP 契约触发存储、向量和语义生命周期，不调用内部实现，不以测试向量或人工状态替代正式路径。

输出直接符合 `ownward.core-baseline-report/v1`，包含候选、二进制、环境与输入绑定以及七项固定内核不变量；底层操作与证据摘要保存在 `details`，不再产生需要二次解释的旧报告格式。
